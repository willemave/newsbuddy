### [HIGH] bug — Auto-skip of already-read podcasts breaks when consecutive items share the same read state
- **File:** `Views/ContentDetailView.swift:455`
- **Issue:** The cascading skip logic relies on `.onChange(of: viewModel.wasAlreadyReadWhenLoaded)` firing every time a new item loads. But `wasAlreadyReadWhenLoaded` is `@Published` and `updateContentId(_:contentType:)` (ViewModels/ContentDetailViewModel.swift lines 69-83) does NOT reset it between loads. When the user chevron/swipe-navigates from a read podcast (value=true) to another already-read podcast (value=true again), SwiftUI's @Published emits no change because the value is identical, so `onChange` never fires. The cascading skip silently stops on the first read item instead of skipping to the next unread one. The same stale value also means navigating from an unread item (false) to an unread item (false) won't reset `didTriggerNavigation`/`navigationDirection`, leaving navigation flags set.
- **Fix:** Reset `wasAlreadyReadWhenLoaded` to a sentinel (or use a fresh nonce/token per load that always changes) inside `updateContentId`, or drive the skip decision off `viewModel.content?.id` changing combined with the read flag rather than relying on a boolean transition. Alternatively make the skip handler read the current value on every content-id change via `.onChange(of: viewModel.content?.id)`.
- **Evidence:** ContentDetailViewModel.swift:40 declares `@Published var wasAlreadyReadWhenLoaded: Bool = false`; the only writes are init (false) and loadContent line 114 `wasAlreadyReadWhenLoaded = fetched.isRead`. updateContentId (lines 69-83) does NOT reset it between loads. The skip logic in ContentDetailView.swift:455 is `.onChange(of: viewModel.wasAlreadyReadWhenLoaded)`, and SwiftUI's onChange fires its closure only when the value actually differs from the prior value. navigateToNext (line 3162) sets didTriggerNavigation=true/direction=+1; on reload the value goes true->true for a second consecutive already-read podcast, so onChange never fires and the cascading skip (lines 457-466) stops on the first read item instead of continuing to the next unread. Real bug; the fix is to reset wasAlreadyReadWhenLoaded in updateContentId so each load re-emits.

### [HIGH] bug — markAsRead reuses an index captured before an await (stale index / wrong-item or crash)
- **File:** `ViewModels/ContentListViewModel.swift:139`
- **Issue:** `index` is captured at line 141 (firstIndex) BEFORE `await contentService.markContentAsRead(...)` at line 143. After the await it is reused at line 145 `contents[index] = ...` and line 160 `contents.remove(at: index)`. During the await, `contents` can be mutated by a concurrent `loadContent()` (each filter `didSet` spawns one) or another `markAsRead`. If `contents` shrank, `remove(at: index)` / subscript can crash with index-out-of-bounds; if it changed order, the wrong item is removed/mutated. This VM is live via FavoritesView (line 126) and RecentlyReadView. Re-resolve the index by id after the await.
- **Fix:** After the await, re-find the item: `guard let i = contents.firstIndex(where: { $0.id == contentId }) else { return }` and use `i` for the subsequent mutate/remove, instead of the pre-await `index`.
- **Evidence:** CONFIRMED. ContentListViewModel.swift markAsRead (139-166): index captured at line 141 'if let index = contents.firstIndex(where: { $0.id == contentId })' BEFORE the await at line 143 'try await contentService.markContentAsRead(...)'. Same index reused after suspension: line 144 'contents[index] = current.updating(isRead: true)' and line 159 'contents.remove(at: index)'. @MainActor but the await is a suspension point during which contents can be mutated by concurrently spawned Tasks (filter didSets at 27/33/39/44 each spawn 'Task { await loadContent() }', and loadContent assigns 'contents = response.contents' at line 84). If contents shrank during suspension, remove(at:index)/subscript can crash out-of-bounds; if reordered, wrong item mutated/removed. VM live in FavoritesView.swift:11, RecentlyReadView.swift:11, ContentListView.swift:11. Fix (re-resolve index by id after await) correct.

### [HIGH] bug — Concurrent loadContent() calls race with no request-generation guard
- **File:** `ViewModels/ContentListViewModel.swift:55`
- **Issue:** Four @Published filters (selectedContentType:25, selectedContentTypes:31, selectedDate:36, selectedReadFilter:41) each fire `Task { await loadContent() }` in didSet. Changing multiple filters (e.g. in FilterSheet) launches several overlapping loadContent() tasks. loadContent() has no generation/cancellation token, so a slower earlier request can finish last and overwrite `contents`/pagination with stale data; both also flip the shared `isLoading` flag, so the first to finish clears the spinner while another is still in flight. BaseContentListViewModel solves exactly this with `requestGeneration` — ContentListViewModel lacks it. loadMoreContent() has the same exposure.
- **Fix:** Add a monotonically-increasing request generation captured at the start of loadContent()/loadMoreContent() and ignore the response (and don't clear isLoading) unless it still matches the latest generation; or cancel the prior Task before starting a new load.
- **Evidence:** CONFIRMED. Four @Published filters fire 'Task { await loadContent() }' in didSet: selectedContentType (25-29), selectedContentTypes (31-35), selectedDate (36-40), selectedReadFilter (41-45). loadContent() (55-93) sets isLoading=true (56), awaits fetchContentList, then unconditionally assigns contents/availableDates/contentTypes/applyPagination (84-87) and isLoading=false (92) with NO generation/cancellation token. Overlapping calls can finish out of order so a stale earlier response overwrites contents; first to finish clears isLoading while another in flight. ContentListViewModel extends CursorPaginatedViewModel (line 12) which has NO requestGeneration (grep shows only resetPagination/applyPagination). Sibling BaseContentListViewModel.swift implements the exact guard: 'private var requestGeneration = 0' (60), 'requestGeneration += 1; let generation = requestGeneration' (197-198), 'guard generation == requestGeneration else { return }' (211, 230). loadMoreContent() (95-137) has same unguarded exposure. Accurate.

### [HIGH] deadcode — Entire QuickMic feature (overlay + context + view model) is dead
- **File:** `Views/Components/QuickMicOverlay.swift:3`
- **Issue:** `QuickMicOverlay` (QuickMicOverlay.swift:3) is never presented anywhere outside its own file. Its companion modifier file QuickMicContext.swift exposes `quickMicContext(...)` and `QuickMicHost`, both with zero external call sites (grep `quickMicContext`, `QuickMicHost`, `QuickMicContextPayload` -> only self-refs). `QuickMicViewModel` (QuickMicViewModel.swift) is referenced only by these two dead files. The whole feature is unwired.
- **Fix:** Delete Views/Components/QuickMicOverlay.swift, Views/Components/QuickMicContext.swift, and ViewModels/QuickMicViewModel.swift together (transitive dead set).
- **Evidence:** Whole-tree grep (newsly + ShareExtension, build dirs excluded): `QuickMicOverlay` -> only QuickMicOverlay.swift:3 (its def). `quickMicContext` -> only QuickMicContext.swift:27 (def). `QuickMicHost` -> only QuickMicContext.swift:46 (def). `QuickMicContextPayload` -> only within QuickMicContext.swift (def + internal uses). `QuickMicViewModel` -> def at QuickMicViewModel.swift:20 plus uses only in the two dead files (QuickMicOverlay.swift:4, QuickMicContext.swift:48). No external call sites anywhere; the entire feature is unwired/dead.

### [HIGH] deadcode — CardStackView and its only-consumed subcomponents (SwipeableCard, PlaceholderCard) are dead
- **File:** `Views/Components/CardStackView.swift:10`
- **Issue:** `struct CardStackView` is referenced nowhere outside its own file (grep `CardStackView` -> only def). It is the SOLE consumer of `SwipeableCard` (SwipeableCard.swift:10, used only at CardStackView.swift:52) and `PlaceholderCard` (PlaceholderCard.swift:10, used only at CardStackView.swift:44). None of the three is reachable from app navigation.
- **Fix:** Delete Views/Components/CardStackView.swift, Views/Components/SwipeableCard.swift, and Views/Components/PlaceholderCard.swift.
- **Evidence:** Grep: `CardStackView` -> only its own def/comments in CardStackView.swift (line 10), no external instantiation. `SwipeableCard` -> def at SwipeableCard.swift:10 and exactly one use at CardStackView.swift:52. `PlaceholderCard` -> def at PlaceholderCard.swift:10 and exactly one use at CardStackView.swift:44. CardStackView is the sole consumer of both subcomponents and is itself unreferenced, so all three are dead together.

### [HIGH] deadcode — LongFormCardStackView (+ ArticleCardView + CardStackKeyPointsLoader) is a dead card-stack chain
- **File:** `Views/Components/LongFormCardStackView.swift:14`
- **Issue:** `struct LongFormCardStackView` has no external references (only its own logger strings). LongFormView uses `LongFormCard` directly, not the stack. `ArticleCardView` (ArticleCardView.swift:13) is consumed ONLY at LongFormCardStackView.swift:95, and `CardStackKeyPointsLoader` (CardStackKeyPointsLoader.swift) is instantiated ONLY at LongFormCardStackView.swift:18. All three die together.
- **Fix:** Delete Views/Components/LongFormCardStackView.swift, Views/Components/ArticleCardView.swift, and ViewModels/CardStackKeyPointsLoader.swift.
- **Evidence:** Grep: `LongFormCardStackView` -> only its own def (line 14) plus its own private logger strings, no external references. `ArticleCardView` -> def at ArticleCardView.swift:13 and exactly one use at LongFormCardStackView.swift:95. `CardStackKeyPointsLoader` -> def at CardStackKeyPointsLoader.swift:21 and exactly one instantiation at LongFormCardStackView.swift:18. Confirmed LongFormView.swift:215 uses `LongFormCard(` directly, not the stack. All three (LongFormCardStackView + ArticleCardView + CardStackKeyPointsLoader) are dead together.

### [HIGH] token — Share Extension uses .systemBlue as selection accent, breaking one-accent doctrine
- **File:** `../ShareExtension/ShareViewController.swift:555`
- **Issue:** The option-row indicator (checkmark) tint is hardcoded to UIColor.systemBlue. Blue is a foreign accent hue; the app's single accent is the terracotta brandPrimary. This is the most visible accent surface in the Share Extension and directly contradicts the one-accent + neutrals color doctrine. The Share Extension is a separate target that does not link DesignTokens/ReaderPalette, so it currently has no shared accent token — but it should still match the app accent rather than fall back to the system blue.
- **Fix:** Introduce a shared brandPrimary UIColor value for the Share Extension (e.g. a small local color constant or shared asset matching #af3535/#e37777) and set indicatorView.tintColor to it instead of .systemBlue. Ideally factor the accent into an asset catalog color shared between the app and extension targets.
- **Evidence:** ShareViewController.swift:555 reads `indicatorView.tintColor = .systemBlue` — the checkmark/circle indicator on each option row. This is the selection accent. Grep across the entire ShareExtension target shows zero references to DesignTokens/brandPrimary/ReaderPalette/terracotta, so the extension is a separate target with no shared accent token; it falls back to system blue. `.systemBlue` is a foreign accent hue used in a content/accent role, a real violation of the one-accent (terracotta brandPrimary) + neutrals doctrine. Confirmed; not an exempt onboarding/landing/watercolor/statusDestructive/SF-symbol case.

### [HIGH] token — Selected option border uses UIColor.systemBlue.cgColor accent
- **File:** `../ShareExtension/ShareViewController.swift:603`
- **Issue:** In updateSelectionState(), the selected option row's layer.borderColor is set to UIColor.systemBlue.cgColor while the unselected state uses UIColor.separator. The selected-state border is an accent role and renders blue, reinforcing the off-doctrine blue accent from the indicator tint (line 555). Should be the app terracotta accent for consistency with the one-accent doctrine.
- **Fix:** Replace UIColor.systemBlue.cgColor with the shared brandPrimary accent color's cgColor (same shared accent introduced for line 555).
- **Evidence:** ShareViewController.swift:603 reads `layer.borderColor = UIColor.systemBlue.cgColor` inside updateSelectionState() for the selected state, while line 606 uses `UIColor.separator.cgColor` for unselected. The selected-row border is an accent role rendered blue, reinforcing the off-doctrine blue from line 555. Same separate-target/no-token situation as idx 0. Real one-accent-doctrine violation.

### [LOW] bug — Tab badge derived by string round-trip can silently zero out
- **File:** `ContentView.swift:106`
- **Issue:** longBadge/shortBadge/moreBadge return an optional String built from a count, then the .badge modifier converts back: `.badge(longBadge != nil ? Int(longBadge!) ?? 0 : 0)`. The value is an Int the whole time, but it is stringified in the computed property and re-parsed with Int(...) and a force-unwrap of the optional String. This is fragile and pointless: any future change that puts a non-numeric badge string (e.g. the knowledgeBadge uses "●") through the same pattern would force-unwrap-then-fail-parse to 0. The knowledgeBadge "●" is correctly not wired to an Int .badge here, but the asymmetry (Int badges via String round-trip, dot badge unused) is bug-prone. Recommend passing Int directly.
- **Fix:** Make longBadge/shortBadge/moreBadge return Int? (0 -> nil) and feed `.badge(count ?? 0)` directly, dropping the String() / Int() round-trip and the force unwrap.
- **Evidence:** Confirmed the round-trip: longBadge/shortBadge/moreBadge are `String?` computed from an Int (lines 41-59, e.g. `return total > 0 ? String(total) : nil`), and .badge re-parses with force-unwrap of the optional String then Int(): lines 106, 133, 173 `.badge(longBadge != nil ? Int(longBadge!) ?? 0 : 0)`. The pattern is genuinely fragile/pointless. However it is not an actual bug today: the only non-numeric badge (knowledgeBadge "●", line 53) is correctly NOT wired to an Int .badge (no `.badge(knowledgeBadge...)` exists). So this is style/fragility, not a live defect — low severity is more accurate than medium.

### [LOW] bug — loadContent staleness guard can miss in-flight type mismatch for mixed lists
- **File:** `ViewModels/ContentDetailViewModel.swift:102`
- **Issue:** `loadContent` snapshots `requestedContentType = contentType` and after the await guards `contentType == requestedContentType`. Because `updateContentId` always sets `contentType` from `initialContentType` (constant) and never changes it across navigation, this guard is effectively only checking `contentId`. That is harmless on its own, but it means the guard provides no protection against the wrong-endpoint problem above; it silently accepts whatever came back. Worth noting alongside the mixed-type endpoint bug.
- **Fix:** Once per-item type is threaded through, ensure the staleness guard compares against the per-item requested type so late responses for a different type are dropped.
- **Evidence:** Confirmed real. ContentDetailNavigationContext.swift:22 declares `let initialContentType: ContentType?` (immutable). ContentDetailView.swift:76 exposes it as a computed passthrough, and the only updateContentId call site (ContentDetailView.swift:431, inside `.task(id: currentIndex)`) always passes that same constant when swiping across the contentIds list. In ContentDetailViewModel updateContentId (line 69-73) only sets self.contentType when newContentType != nil, but it's always the same value. So the staleness guard at lines 102-103 (`contentType == requestedContentType`) is trivially always-true and only contentId is actually validated. The finding's framing (harmless on its own, but no protection against wrong-endpoint for mixed-type lists) matches the code. Severity low correct — this is a noted limitation, not an active crash/data bug.

### [LOW] bug — markAsRead/markAsUnread trigger a full article reload, duplicating network work
- **File:** `ViewModels/ArticleDetailViewModel.swift:44`
- **Issue:** `markAsRead()` and `markAsUnread()` call the mutation endpoint and then `await loadArticle(id:)` to 'refresh' (lines 48-50, 60-62). `loadArticle` re-fetches the full content detail AND the full content body again, even though only the read flag changed. This duplicates an expensive body fetch on every read-state toggle. (Note: this view model appears largely unused relative to ContentDetailViewModel; verify it is still wired before optimizing.)
- **Fix:** Mutate `article?.isRead` locally after the successful mark call instead of reloading the entire article + body, or add a lightweight refresh path.
- **Evidence:** markAsRead (lines 44-54) calls markContentAsRead then `await loadArticle(id:)` (line 50); markAsUnread (lines 56-66) does the same (line 62). loadArticle (lines 22-42) re-fetches the full content detail (fetchContentDetail line 27) AND the full body (fetchContentBody line 32 when bodyAvailable) — an expensive re-fetch on every read-state toggle. The finding's note to verify wiring is borne out: grep for `ArticleDetailViewModel` across newsly+ShareExtension (excluding build dirs) returns only its own file (line 2 comment, line 12 class decl) — the class is not referenced/instantiated anywhere, so the duplicate-fetch is dead in practice. Real but minor; low severity appropriate.

### [LOW] bug — Podcast transcript-expandable section is unreachable when bodyText is present
- **File:** `Views/ContentDetailView.swift:342`
- **Issue:** The Full Content block (lines 332-362) uses an `if / else if` chain. The first branch fires for `contentTypeEnum != .news` when `viewModel.contentBody?.text` exists, which is true for podcasts with a loaded body. The second branch (`else if contentTypeEnum == .podcast, let transcript = podcastMetadata.transcript`) can therefore never run for a podcast whose body has loaded, even if the metadata transcript differs from the body text. This is likely intended fallback ordering, but confirm the podcast transcript path is reachable when both a body and a metadata transcript exist.
- **Fix:** If the metadata transcript should take precedence (or supplement) the generic body for podcasts, reorder the conditions so the podcast-specific branch is evaluated before the generic `contentBody?.text` branch.
- **Evidence:** ContentDetailView.swift lines 332-362 is an if / else-if chain. Branch 1 (line 332): `if content.contentTypeEnum != .news, let bodyText = viewModel.contentBody?.text` — fires for podcasts (.podcast != .news) once a body is loaded. Branch 2 (line 342): `else if content.contentTypeEnum == .podcast, let podcastMetadata = ..., let transcript = podcastMetadata.transcript`. Because branch 1 already captured all non-news with a loaded body, branch 2 is unreachable for a podcast whose body text is present, even if the metadata transcript differs. Confirmed unreachable as described; finding itself flags it as likely-intended fallback ordering, low severity appropriate.

### [LOW] bug — Inline emphasis inside headings is flattened by applyHeadingStyle
- **File:** `Views/Components/SelectableMarkdownView.swift:548`
- **Issue:** renderInline(text) correctly applies bold/italic/code runs for a heading's text, but applyHeadingStyle then does `attrStr.addAttribute(.font, value: styledHeadingFont, range: <entire range>)`, overwriting every per-run font with the single heading font. So `## Some **bold** word` renders with uniform weight and loses the bold/italic/code styling inside the heading. Lower-severity since headings rarely contain inline emphasis, but it is a real rendering correctness loss.
- **Fix:** In applyHeadingStyle, enumerate existing .font runs and merge the heading size/weight while preserving symbolic traits (italic) and existing weight, instead of blanket-overwriting .font across the whole range; or apply heading scaling inside renderInline so per-run traits survive.
- **Evidence:** Headings render via renderInline first then applyHeadingStyle: line 225 `let rendered = renderInline(text)`, line 226 `applyHeadingStyle(to: rendered, level: level)`. renderInline (line 618+) assigns per-run fonts for bold/italic/code (e.g. lines 635-648). applyHeadingStyle (line 533) computes `range = NSRange(location: 0, length: attrStr.length)` (line 534, entire string) and at line 548 `attrStr.addAttribute(.font, value: styledHeadingFont, range: range)` overwrites every per-run font with the single heading font. So `## Some **bold** word` loses inline emphasis. Real rendering-correctness loss; low severity appropriate.

### [LOW] bug — utcMicrosecondsFormatter dateFormat uses 6 'S' but DateFormatter only supports millisecond precision
- **File:** `Views/Components/ContentTimestampText.swift:31`
- **Issue:** dateFormat "yyyy-MM-dd'T'HH:mm:ss.SSSSSS" requests microsecond precision, but Foundation's DateFormatter (NSDateFormatter / ICU) only resolves fractional seconds to milliseconds; the extra S digits are not parsed as sub-millisecond and can cause parse failures or misinterpretation for strings whose fractional part does not have exactly the expected digit count. Because this is the 3rd fallback after the ISO8601 fractional/non-fractional formatters, the practical blast radius is small, but a backend timestamp like `2026-06-08T12:00:00.123456` (no 'Z', 6-digit fraction) relies on this path and the behavior is fragile across OS versions.
- **Fix:** Either drop to millisecond precision (".SSS") with a separate fallback, or strip the fractional component and parse to seconds, or normalize the raw string (truncate fraction to 3 digits) before parsing. Confirm against the exact backend format for created_at/processed_at.
- **Evidence:** ContentTimestampText.swift line 31: `formatter.dateFormat = "yyyy-MM-dd'T'HH:mm:ss.SSSSSS"` (6 'S'). Foundation DateFormatter/ICU resolves fractional seconds only to millisecond precision; a 6-digit fraction can be misinterpreted or fail to parse depending on OS. parse() tries this only as the 3rd fallback (line 79) after iso8601WithFractionalFormatter (line 71) and iso8601Formatter (line 75), so blast radius is small — only non-'Z' 6-digit-fraction strings reach it. The finding self-marks low and explicitly acknowledges the small blast radius and the speculative misinterpretation; structural claims (3rd fallback, 6 'S') are all confirmed. Low severity appropriate.

### [LOW] bug — Debug print() statements left in production view body/task
- **File:** `Views/Components/PagedCardView.swift:77`
- **Issue:** Lines 77, 89 emit raw print("📏 ...") and print("🃏 ...") inside onPreferenceChange and a .task. These run on every layout/height change in the hot path of the paged feed, spamming the console in release builds and adding overhead. Other files in this group use os.Logger; these should too or be removed.
- **Fix:** Remove the print() calls or route them through a Logger at .debug level.
- **Evidence:** PagedCardView.swift:77 `print("📏 Intrinsic content height: ...")` is inside onPreferenceChange (line 71-78) and line 89 `print("🃏 Reporting cardHeight: ...")` is inside .task(id: cardHeight) (line 82-91). grep confirms these are the only two print() calls in the card-stack group; sibling files LongFormCardStackView.swift and ArticleCardView.swift both use os.Logger (e.g. `private let cardLogger = Logger(...)`). Both have guards limiting fire frequency (abs delta < 2 early-returns), but they are raw print() left in a production view body/task. Low severity correct.

### [LOW] bug — LongformArtifactKeyPoint.id can collide and break ForEach identity
- **File:** `Models/LongformArtifact.swift:67`
- **Issue:** id is `heading + String(content.prefix(20))`. Two key points that share a heading and whose content agrees in the first 20 characters produce the same id. KeyPointList renders ForEach(points) (line 116) using this Identifiable id, so duplicates would cause SwiftUI to drop/merge rows or animate incorrectly. LongformArtifactQuote.id == text (line 60) has the same risk for repeated quotes.
- **Fix:** Make the id incorporate the array offset (e.g. enumerate in the ForEach with id: \.offset) or hash the full heading+content rather than a truncated prefix.
- **Evidence:** LongformArtifact.swift:67 `var id: String { heading + String(content.prefix(20)) }` and line 60 `var id: String { text }` for the quote. These conform to Identifiable. KeyPointList renders `ForEach(points) { point in` at LongformArtifactView.swift:116 using the Identifiable id. Two key points sharing heading + first-20-chars-of-content collide; repeated quote text collides. Real but low-probability ForEach identity risk; low severity correct.

### [LOW] bug — Load-more guard reads currentIndex after it was advanced, off by one slot vs intent
- **File:** `Views/Components/LongFormCardStackView.swift:226`
- **Issue:** After the withAnimation closure sets currentIndex = targetIndex, line 226 checks `if currentIndex >= items.count - 3`. This reads the new currentIndex (intended), but combined with the haptic bug above it shows the function mixes pre- and post-mutation reads of currentIndex. It works today but is fragile; if someone later moves the load-more check before the assignment it would silently change behavior. Worth normalizing to use an explicit `targetIndex`/`oldIndex` snapshot like PagedCardView does.
- **Fix:** Use `if targetIndex >= items.count - 3 { viewModel.loadMoreTrigger.send(()) }` so the trigger does not depend on read-after-write ordering.
- **Evidence:** LongFormCardStackView.swift:217-220 `withAnimation(...) { currentIndex = targetIndex; dragOffset = 0 }` mutates currentIndex, then line 226 `if currentIndex >= items.count - 3 { viewModel.loadMoreTrigger.send(()) }` reads the post-mutation value. The finding's premise is verified — and notably the cited 'haptic bug above' is real: line 222 `if targetIndex != currentIndex { triggerHaptic() }` runs AFTER currentIndex was set to targetIndex, so the condition is always false and the haptic never fires. The load-more check itself works as intended today; the fragility/mixed pre-post-mutation-read concern is accurate. Low severity fair for the normalization suggestion.

### [LOW] bug — ForEach keyed on \.text risks duplicate-ID identity glitches
- **File:** `Views/Components/StructuredSummaryView.swift:43`
- **Issue:** Multiple ForEach loops use text as the identity: quotes id:\.text (line 43), bulletPoints id:\.text (line 59), and similarly InterleavedSummaryV2View keyPoints/quotes id:\.text (lines 30,39,55) and BulletedSummaryView quotes id:\.text (line 56). Quote/BulletPoint are not Identifiable so text is the only stable key, but if the model returns two bullets or two quotes with identical text (plausible for short repeated phrases) SwiftUI ForEach sees duplicate IDs, which produces undefined rendering (dropped/duplicated rows) and a runtime warning. InterleavedInsight.id = topic + insight.prefix(20) (StructuredSummary.swift line 59) has the same collision risk for two insights sharing a topic and prefix.
- **Fix:** Use Array(...).enumerated() with offset, or give these models a stable unique id (e.g. include array index in the id), so identical text strings don't collide.
- **Evidence:** BulletPoint (StructuredSummary.swift:34) and Quote (line 39) are 'Codable' only, NOT Identifiable. ForEach keyed on \.text exists exactly as cited: StructuredSummaryView.swift:43 (quotes) & :59 (bulletPoints); InterleavedSummaryV2View.swift:30 (keyPoints), :39 (quotes), :55 (topic.bullets); BulletedSummaryView.swift:56 (point.quotes). InterleavedInsight.id = topic + insight.prefix(20) at StructuredSummary.swift:59. Two items with identical text (or topic+20-char prefix) yield duplicate SwiftUI IDs => undefined rendering + runtime warning. Real but uncommon, so 'low' severity is apt.

### [LOW] bug — Topic-chat failures only print, no user-facing error
- **File:** `Views/Components/StructuredSummaryView.swift:294`
- **Issue:** startChatSession catches all errors and does `print("Failed to start topic chat: \(error)")` returning nil (line 294). On failure the sheet simply never appears with no feedback to the user, and print is not the structured logging used elsewhere. A silently failing async action that was user-initiated (tapping Deep Dive) gives no indication anything happened.
- **Fix:** Surface a lightweight error state (e.g. a toast/alert) on failure, and replace print with the os.Logger used elsewhere in the codebase.
- **Evidence:** startChatSession (StructuredSummaryView.swift:285-297) catches all errors with print("Failed to start topic chat: \(error)") at line 294 (the file's only print and only error handling) and returns nil. Callers startTopicChat (269-275) and startKeyPointChat (277-283) only set topicSession when a non-nil session returns, so on failure the .sheet(item: $topicSession) never presents — no user feedback for a user-initiated Deep Dive tap. App has structured logging (AppLog used widely in ViewModels), so raw print is also inconsistent. Confirmed; 'low' is appropriate.

### [LOW] bug — edgeBackSwipeGesture uses deprecated UIScreen.main and a delay decoupled from the animation
- **File:** `Views/ChatSessionView.swift:161`
- **Issue:** The custom edge-back DragGesture animates edgeBackDragOffset to UIScreen.main.bounds.width (deprecated multi-scene API; returns the wrong width in split-screen/multi-window and on iPad), then dismisses inside a hard-coded DispatchQueue.main.asyncAfter(deadline: .now() + 0.18) that must be manually kept in sync with the 0.18s animation. If the value/animation duration ever drift, the dismiss fires mid-animation or after a visible pause. The asyncAfter closure also fires unconditionally even if the view was already torn down, and resets edgeBackDragOffset = 0 after dismiss (potential flash).
- **Fix:** Resolve width from the view's own geometry (GeometryReader / containerRelativeFrame) instead of UIScreen.main, and drive dismissal from the animation completion (withAnimation { } completion: { dismiss() } on iOS 17+) rather than a hand-tuned asyncAfter delay. Consider whether the system interactive-pop already covers this and the custom gesture is needed at all.
- **Evidence:** ChatSessionView.swift lines 160-166: `withAnimation(.easeOut(duration: 0.18)) { edgeBackDragOffset = UIScreen.main.bounds.width }` then `DispatchQueue.main.asyncAfter(deadline: .now() + 0.18) { dismiss(); edgeBackDragOffset = 0 }`. UIScreen.main is the deprecated multi-scene API (returns wrong width in split-screen/iPad). The 0.18s asyncAfter delay is manually duplicated from the 0.18s animation duration — drift causes mid-animation/late dismiss. Closure resets edgeBackDragOffset=0 after dismiss (flash) and fires unconditionally. Only one other UIScreen.main.bounds.width content use exists; grep confirms this is the spot. All claims accurate; low severity appropriate.

### [LOW] bug — Source row renders for empty-string source (missing isEmpty guard)
- **File:** `Views/Chat/ArticlePreviewCard.swift:21`
- **Issue:** The source branch uses only `if let source` while the summary branch below correctly uses `if let summary, !summary.isEmpty`. If the API returns a non-nil but empty/whitespace articleSource, an otherwise-empty HStack renders showing just the 'doc.text' icon and a blank label, adding stray spacing. Inconsistent with the summary handling in the same view.
- **Fix:** Match the summary guard: `if let source, !source.isEmpty`. Optionally trim whitespace before the check.
- **Evidence:** ArticlePreviewCard.swift line 21: `if let source {` with no isEmpty check, rendering HStack with `Image(systemName: "doc.text")` + `Text(source)` (lines 22-28). Line 31 directly below uses `if let summary, !summary.isEmpty`. The inconsistency is real: a non-nil empty/whitespace source renders the icon plus a blank label and stray spacing. Accurately described.

### [LOW] bug — Vertical TextField has no send-on-return affordance; Return only inserts newlines
- **File:** `Views/Chat/ChatComposerDock.swift:143`
- **Issue:** The composer TextField uses axis: .vertical with lineLimit(1...5) and no .onSubmit handler. With a vertical-axis TextField the Return key inserts a newline rather than submitting, so the only way to send is the dedicated send button. That is a defensible product choice, but there is no onSubmit/keyboard send path at all, which is worth confirming is intentional rather than an omission (users on hardware keyboards cannot send with Return).
- **Fix:** If send-on-return is desired, detect a trailing newline or add a submitLabel/onSubmit path that calls onSend when not multiline. If newline-on-return is intended, no change needed — confirm explicitly.
- **Evidence:** ChatComposerDock.swift line 143: `TextField("Message", text: $inputText, axis: .vertical)` with `.lineLimit(1...5)` (line 146). grep for onSubmit/submitLabel/.onSubmit in the file returned no output — no submit handler exists. Vertical-axis TextField makes Return insert a newline; only the send button submits, so hardware-keyboard Return cannot send. Finding is a 'confirm intentional' note and accurately describes the code; low severity appropriate.

### [LOW] bug — PendingSend placeholder message id derived from truncated UUID can collide / parse to 0
- **File:** `ViewModels/ChatTimelineReconciler.swift:25`
- **Issue:** `placeholderMessageId` parses the first 8 hex chars of the UUID string as an Int (`Int(prefix, radix: 16) ?? 0`). Two pending sends can collide (1 in ~4 billion) and any UUID whose first 8 hex digits are not parseable falls back to 0. Placeholder rows use `.local(localId)` for ChatTimelineID identity so this Int is not the primary key, but it is the message `.id` (Identifiable) and feeds `isOrderedBefore`'s tiebreaker `sourceMessageId ?? id`. Collisions would only perturb sort ordering of two simultaneous unsent placeholders, so impact is minor.
- **Fix:** Use a stable monotonic placeholder id (e.g. a decrementing negative counter) instead of a truncated UUID hash, or order placeholders purely by createdAt + localId.
- **Evidence:** ViewModels/ChatTimelineReconciler.swift:25-28: placeholderMessageId = Int(localId.uuidString.prefix(8), radix:16) ?? 0, and is assigned to ChatMessage.id (line 16) with sourceMessageId: nil (line 17). ChatTimelineItem.isOrderedBefore (Models/ChatTimelineItem.swift:64) tiebreaks on `message.sourceMessageId ?? message.id`, so for placeholders this resolves to the truncated-UUID Int. Collision (1 in ~4B) or non-parseable prefix->0 would perturb sort ordering of simultaneous unsent placeholders only; .local(localId) is the row identity key (line 52). Minor/low impact as stated.

### [LOW] bug — Council-selection timeout error message can linger briefly after success
- **File:** `ViewModels/ChatSessionViewModel.swift:723`
- **Issue:** The deadline task sets `errorMessage = "Switching perspectives is taking longer than expected."` and `councilSelectionTimedOut = true` at 10s. The success path clears `errorMessage = nil` (line 667) and the `defer` resets `councilSelectionTimedOut = false`, so a normal late success self-heals. However if the deadline fires and the request then resolves via the cancellation branch (isCancelledOperation), neither errorMessage nor the timeout flag is re-cleared by that branch — only the defer clears the flag, leaving the stale errorMessage shown until the next action. Narrow timing window.
- **Fix:** Clear `errorMessage` in the defer (or in the cancellation catch) when `selectCouncilRequestId == requestId`, so a timed-out-then-cancelled selection does not leave a stale banner.
- **Evidence:** ChatSessionViewModel.swift: deadline task (startCouncilSelectionDeadline, lines 713-725) sets councilSelectionTimedOut=true AND errorMessage='Switching perspectives is taking longer than expected.' (lines 722-723) when selectCouncilRequestId still matches. The cancellation branch (line 668-669) only logs .debug and does NOT clear errorMessage. The defer (lines 649-658) clears councilSelectionTimedOut=false but never touches errorMessage. Success path (667) and generic error path (671) both set errorMessage, but a deadline-fire-then-cancel sequence leaves the stale timeout errorMessage shown until next action. Real but very narrow timing window; low severity correct.

### [LOW] bug — startVoiceRecording lacks re-entrancy guard unlike its chat counterpart
- **File:** `ViewModels/TweetSuggestionsViewModel.swift:182`
- **Issue:** Unlike ChatSessionViewModel.startVoiceRecording (which guards `!isRecording, !isTranscribing`), this method has no guard before calling `transcriptionService.start()`. The voice button toggles on `isRecording`, so the UI mostly prevents double-start, but a rapid double tap before `isRecording` flips true (the start is async) can call `start()` twice on the shared VoiceDictationService. The service's own `startRecording` has a `guard !isRecording` so the impact is limited, but the second await still sets `isRecording = true` again and logs a spurious start.
- **Fix:** Add `guard !isRecording, !isTranscribing else { return }` at the top of startVoiceRecording.
- **Evidence:** TweetSuggestionsViewModel.startVoiceRecording (line 182) calls transcriptionService.start() (line 187) with NO re-entrancy guard before it. The chat counterpart ChatSessionViewModel.startVoiceRecording (line 778) opens with `guard !isRecording, !isTranscribing else { return }` (line 779). UI button toggles on isRecording but isRecording is set only after the async start() returns (line 188), so a rapid double-tap can invoke start() twice; underlying VoiceDictationService guard limits damage. Confirmed, low severity correct.

### [LOW] bug — completionPrimaryTitle hides selected long-form count when no short-form sources are picked
- **File:** `Views/Onboarding/OnboardingFlowView.swift:1013`
- **Issue:** completionPrimaryTitle returns 'Start reading' whenever selectedShortformCount == 0, even if the user has selected several newsletters/podcasts (selectedLongformCount > 0). The total-count label that says 'Start with N sources' is only shown when at least one aggregator/subreddit is selected, so a longform-only selection misreports as if nothing was chosen.
- **Fix:** Base the title on the combined total: if (selectedShortformCount + selectedLongformCount) == 0 return 'Start reading', else 'Start with \(total) sources'. Confirm this matches intended UX with the designer.
- **Evidence:** OnboardingFlowView.swift L1013-1018: completionPrimaryTitle returns 'Start reading' whenever `selectedShortformCount == 0` (L1014), ignoring selectedLongformCount. selectedShortformCount = aggregators+subreddits (L1009-1011); selectedLongformCount = selectedSourceKeys (L1005-1007) — disjoint sets. So a longform-only selection (e.g. 3 newsletters, 0 aggregators/subreddits) shows 'Start reading' instead of 'Start with 3 sources'. Rendered as the primary button label at L612. Real cosmetic/labeling bug; low severity correct.

### [LOW] bug — Council expert ForEach id can collide, breaking list identity
- **File:** `Views/Settings/SettingsView.swift:269`
- **Issue:** CouncilPersona.id is derived from the name (User.swift lines 28-37: lowercased, whitespace-joined, truncated to 50 chars). addExpert() (SettingsView.swift line 382) does not check for duplicates, so adding the same expert twice — or two names that normalize to the same id (e.g. 'Paul Graham' and 'paul-graham' both differ, but 'Paul Graham' added twice yields id 'paul_graham' twice) — produces duplicate ids in councilPersonasDraft. ForEach(Array(councilPersonasDraft.enumerated()), id: \.element.id) (line 269) then has non-unique ids, which causes SwiftUI identity warnings and incorrect row/removal behavior (removeExpert(at: index) can target the wrong logical row after a re-render).
- **Fix:** Reject duplicates in addExpert() by checking councilPersonasDraft for an existing persona with the same id (or same normalized displayName) before appending, and surface a brief inline message. Alternatively give CouncilPersona a stable UUID id rather than a name-derived one.
- **Evidence:** CouncilPersona.id is derived from the name in User.swift:28-37 (init(name:): id = name.lowercased().components(separatedBy:.whitespaces).joined(separator:"_").prefix(50)). SettingsView.swift:382-389 addExpert() has no duplicate check: it just appends CouncilPersona(name: name, ...). ForEach at SettingsView.swift:269 uses id: \.element.id. Adding 'Paul Graham' twice yields two personas with id 'paul_graham', producing non-unique ForEach ids -> SwiftUI identity warnings and removeExpert(at: index) at line 391 can target the wrong logical row. maxExperts=3 (User.swift:16) bounds the impact, so low severity is correct.

### [LOW] bug — loadSuggestions has no re-entrancy guard; onAppear + refreshable can run concurrent fetches
- **File:** `ViewModels/DiscoveryViewModel.swift:85`
- **Issue:** KnowledgeDiscoveryView calls viewModel.loadSuggestions() in .onAppear and loadSuggestions(force:true) in .refreshable. loadSuggestions() sets isLoading = true with no guard against an already-in-flight call, so two fetchHistory() requests can overlap and race when applying results (apply() overwrites runs/feeds/podcasts). hasLoaded only short-circuits the non-forced path after the first completes, not while one is in flight. (Note: this VM/view is currently unreferenced, so impact is latent.)
- **Fix:** Add a guard like `guard !isLoading else { return }` at the top of loadSuggestions (mirroring loadHub() in KnowledgeHubViewModel), or track an in-flight Task and cancel/await it.
- **Evidence:** DiscoveryViewModel.swift:85-99 loadSuggestions(force:) only short-circuits via 'if hasLoaded && !force { return }'; it sets isLoading=true with no in-flight guard, and hasLoaded is set to true only AFTER fetchHistory() completes (line 94). KnowledgeDiscoveryView.swift:27 calls loadSuggestions() in onAppear and line 30 calls loadSuggestions(force:true) in refreshable, so concurrent fetches can overlap and apply()/race. Latent: grep shows DiscoveryViewModel is referenced only by its own definition and KnowledgeDiscoveryView, and KnowledgeDiscoveryView itself has no external references, so no live caller. Low severity correct.

### [LOW] bug — refreshDiscovery queues a run but never starts polling, so the running banner never resolves on its own
- **File:** `ViewModels/DiscoveryViewModel.swift:101`
- **Issue:** refreshDiscovery() sets runStatus = response.status (e.g. "queued") and hasLoaded = false, which makes isJobRunning true and shows the 'Discovering...' banner. Nothing polls discoveryStatus afterward, so the UI stays in the running state until the user manually pulls to refresh (which re-runs loadSuggestions). Unlike DiscoveryPersonalizeViewModel.startPolling, there is no automatic status refresh loop here.
- **Fix:** After a successful refresh(), start a bounded polling loop that calls fetchHistory()/a status endpoint until runStatus is completed/failed, or document that manual refresh is required.
- **Evidence:** DiscoveryViewModel.swift:101-117 refreshDiscovery() sets runStatus = response.status and hasLoaded=false, then returns. isJobRunning (lines 43-47) returns true for status in [queued,pending,processing,running], so the running banner shows. No polling loop is started, unlike DiscoveryPersonalizeViewModel.startPolling (DiscoveryPersonalizeViewModel.swift:216-234) which spawns a polling Task that re-checks discoveryStatus. So the UI stays 'running' until the user pulls to refresh. Latent (unreferenced VM/view). Low severity correct.

### [LOW] bug — currentJobStage returns 4 for 'completed' but progress dots only render indices 0..<4
- **File:** `ViewModels/DiscoveryViewModel.swift:79`
- **Issue:** currentJobStage maps 'completed' to 4, but DiscoveryProcessingStateView renders ForEach(0..<4) dots and uses scaleEffect(index == currentJobStage ? pulseScale : 1.0). Stage 4 is out of the dot range, so no dot pulses at completion. In practice DiscoveryProcessingStateView is only shown while isJobRunning (never 'completed'), so this is latent, but the 4 value is inconsistent with the 0..<4 dot model.
- **Fix:** Clamp completed to 3 (last dot) or change the dot range to 0..<5 to match the stage model; align currentJobStage's max with the rendered dot count.
- **Evidence:** DiscoveryViewModel.swift:78-79 maps `case "completed": return 4`. DiscoveryStateViews.swift:139 `ForEach(0..<4, id: \.self)` renders indices 0..3; line 141 `index <= currentJobStage` (all dots filled at 4) and line 143 `scaleEffect(index == currentJobStage ? pulseScale : 1.0)` (no dot pulses since no index equals 4). KnowledgeDiscoveryView.swift:89 shows the view only `!viewModel.hasSuggestions && viewModel.isJobRunning`, so 'completed' status never reaches this view — confirming the latent/cosmetic nature the finding describes. Severity low is accurate.

### [LOW] bug — 30s auto-stop timer and silence/manual stop can race to call stopAudioCaptureAndDiscover
- **File:** `ViewModels/DiscoveryPersonalizeViewModel.swift:437`
- **Issue:** handleAudioTimerTick() fires stopAudioCaptureAndDiscover() once audioDurationSeconds >= 30 while still .recording. If the user taps stop near the 30s mark, both the timer tick and the manual onStop can drive a stop. stopAudioCaptureAndDiscover sets audioState=.transcribing and calls dictationService.stop(); a second concurrent stop() call on the underlying VoiceDictationService throws recordingFailed (guard !isFinalizing). The error path then calls handleAudioError, surfacing a spurious error to the user even though discovery may already be proceeding. beginDiscovery itself is guarded against double submission, but the stop() double-call error is not.
- **Fix:** Guard stopAudioCaptureAndDiscover with a flag (e.g. ignore if audioState != .recording) so the timer and manual stop cannot both invoke dictationService.stop().
- **Evidence:** stopAudioCaptureAndDiscover() (DiscoveryPersonalizeViewModel.swift:120) has NO re-entry guard on audioState; it unconditionally sets audioState=.transcribing (122), stops the timer (123), and awaits dictationService.stop() (128). Two independent call paths exist: handleAudioTimerTick (435-439) dispatches `Task { await stopAudioCaptureAndDiscover() }` when `audioDurationSeconds >= 30 && audioState == .recording`, and the manual onStop (DiscoveryPersonalizeSheet.swift:80) dispatches its own Task. The timer-tick guard reads `.recording` synchronously and dispatches the Task before any state change lands — a classic TOCTOU window near the 30s mark. A second stop() reaches finalizeRecordingAndTranscribe (VoiceDictationService.swift:303) whose guards `isRecording` (305) and `!isFinalizing` (308) throw VoiceDictationError.recordingFailed once the first call set isRecording=false/isFinalizing=true (319,312). That throw propagates through stop() (144-150, calls onError + rethrows), is caught at line 132, and handleAudioError (331-335) sets audioState=.error and surfaces errorMessage — a spurious user-facing error. Mechanism and consequence are real; low severity is appropriate given the narrow timing window.

### [LOW] bug — Debug print() statements with emoji left in production code
- **File:** `ViewModels/NewsGroupViewModel.swift:62`
- **Issue:** loadNewsGroups uses `print("🧮 Fetch news groups …")` (62) and `print("🧮 Fetch returned …")` (73). The rest of these view models use os.log Logger. Raw print() is unstructured, always-on logging cruft that should not ship.
- **Fix:** Replace with the existing Logger (logger.debug/info) or remove.
- **Evidence:** NewsGroupViewModel.swift:62 `print("🧮 Fetch news groups — size: ...")` and line 73 `print("🧮 Fetch returned ... items → ... groups ...")`. Both are raw emoji-prefixed print() statements inside loadNewsGroups, unstructured always-on logging that ships in release builds. Confirmed exactly as described; low severity appropriate.

### [LOW] bug — Detached audio/quick-action Tasks are not cancelled when the view goes away
- **File:** `Views/ShortFormView.swift:206`
- **Issue:** handleFastNewsAudioEpisode() (206) and startQuickAction() (383) launch unstored `Task { @MainActor in … }`. Their `catch where isNetworkCancellation(error)` paths assume cancellation, but nothing cancels these tasks when ShortFormView disappears or when isActive flips, so network/episode work continues and flags (isPreparingFastNewsAudio / activeQuickActionId) only reset when the task itself completes. Not a crash (View is a value type, no retain cycle), but work isn't tied to view lifetime.
- **Fix:** Hold these in stored Task handles and cancel on disappear / when starting a new one, or drive them from .task(id:) so SwiftUI cancels them automatically.
- **Evidence:** ShortFormView.swift: handleFastNewsAudioEpisode (line 196) sets isPreparingFastNewsAudio=true (203) then launches unstored `Task { @MainActor in ... }` (line 206) with `defer { isPreparingFastNewsAudio = false }`. startQuickAction (line 377) does the same with `Task { @MainActor in ... }` (line 383) and `defer { activeQuickActionId = nil }`. Neither task is stored, bound via .task, nor cancelled on disappear/isActive change. The `catch where isNetworkCancellation(error)` (lines 237, 392) handle cancellation but nothing triggers it tied to view lifetime. Work continues until the task completes on its own. No crash/retain cycle (value-type View). Severity low accurate.

### [LOW] bug — Non-numeric episode-limit input is silently treated as 'no limit'
- **File:** `Views/Sources/PodcastSourcesView.swift:184`
- **Issue:** `let limitValue = Int(trimmedLimit)` returns nil for any non-numeric entry (e.g. 'abc'). The range check only runs `if let limitValue, !(1...100)...`, so non-numeric, non-empty input bypasses validation entirely and the config is created with `limit: nil` with no feedback. A user who fat-fingers the field gets a silently different result than intended.
- **Fix:** Distinguish empty vs non-empty-but-unparseable: if `!trimmedLimit.isEmpty && Int(trimmedLimit) == nil`, set `localError = "Limit must be a number between 1 and 100"` and return.
- **Evidence:** PodcastSourcesView.swift line 184: `let limitValue = Int(trimmedLimit)` returns nil for non-numeric input. Line 186: `if let limitValue, !(1...100).contains(limitValue)` only validates when conversion succeeds. So a non-empty, non-numeric entry (e.g. 'abc') → limitValue=nil → bypasses range check → addConfig called with `limit: nil` (line 197) with no error feedback. Real silent-failure UX bug. (Mitigated somewhat by keyboardType .numberPad at line 163, but paste/non-numeric still possible.) Severity low accurate.

### [LOW] deadcode — Unused back-compat Color tokens textPrimary/textSecondary/textTertiary
- **File:** `Views/Shared/DesignTokens.swift:90`
- **Issue:** `Color.textPrimary` (line 90), `Color.textSecondary` (91), `Color.textTertiary` (92) are back-compat aliases; grep whole-word across the tree finds zero usages anywhere except their own definitions. Callers now use onSurface/onSurfaceSecondary/onSurfaceTertiary directly.
- **Fix:** Remove the three textPrimary/textSecondary/textTertiary computed properties (DesignTokens.swift:90-92).
- **Evidence:** DesignTokens.swift:89-92 defines back-compat aliases textPrimary (90, =Color.onSurface), textSecondary (91, =Color.onSurfaceSecondary), textTertiary (92, =Color.onSurfaceTertiary). Whole-tree grep across newsly + ShareExtension (excluding build/dep dirs) returns exactly ONE hit per symbol — its own definition line. Zero external callers. Confirmed dead code. Low severity.

### [LOW] deadcode — Unused summary accent Color tokens summaryPrimaryAccent and summaryQuoteAccent
- **File:** `Views/Shared/DesignTokens.swift:130`
- **Issue:** `Color.summaryPrimaryAccent` (line 130) and `Color.summaryQuoteAccent` (line 134) have zero references anywhere outside their definitions (grep whole-word -> def only). The other summary*Accent tokens (Secondary/Question/Counterpoint) still have call sites; these two do not.
- **Fix:** Remove summaryPrimaryAccent (line 130) and summaryQuoteAccent (line 134).
- **Evidence:** grep whole-source: `summaryPrimaryAccent` appears ONLY at DesignTokens.swift:130 (its def); `summaryQuoteAccent` ONLY at DesignTokens.swift:134 (its def). Siblings DO have call sites: summarySecondaryAccent -> EditorialNarrativeSummaryView.swift:47; summaryQuestionAccent -> InterleavedSummaryV2View.swift:46 + StructuredSummaryView.swift:77; summaryCounterpointAccent -> StructuredSummaryView.swift:93. Both flagged tokens are dead (def-only). Severity low is correct (cosmetic dead token, all == brandPrimary).

### [LOW] deadcode — Unused onboardingAmbientSecondary alias
- **File:** `Views/Shared/DesignTokens.swift:166`
- **Issue:** `Color.onboardingAmbientSecondary` (line 166), defined as an alias to onboardingAmbientTertiary, is referenced nowhere (grep whole-word -> def only). The Primary/Tertiary/Quaternary ambient tokens are used; this Secondary alias is not.
- **Fix:** Remove the onboardingAmbientSecondary computed property (line 166).
- **Evidence:** `onboardingAmbientSecondary` appears ONLY at DesignTokens.swift:166 (its def, aliasing onboardingAmbientTertiary). Primary/Tertiary/Quaternary each have multiple real call sites (e.g. WatercolorBackground.swift, OnboardingFlowView.swift, OnboardingSuggestionCard.swift, OnboardingMicButton.swift). The Secondary alias is dead. Severity low correct.

### [LOW] deadcode — Unused feed/card Font tokens feedSnippet, cardDescription, cardBadge, cardFooter
- **File:** `Views/Shared/DesignTokens.swift:252`
- **Issue:** `Font.feedSnippet` (252), `Font.cardDescription` (254), `Font.cardBadge` (255), `Font.cardFooter` (256) each appear only at their own definition (grep whole-word -> def only). No view applies them.
- **Fix:** Remove feedSnippet, cardDescription, cardBadge, cardFooter from the Font extension.
- **Evidence:** Substring grep across whole tree: feedSnippet only at DesignTokens.swift:252; cardDescription only at :254; cardBadge only at :255; cardFooter only at :256 — all def-only, zero applications. Confirmed dead. Severity low correct.

### [LOW] deadcode — Unused editorial Font tokens editorialDisplay and editorialHeadline
- **File:** `Views/Shared/DesignTokens.swift:259`
- **Issue:** `Font.editorialDisplay` (259) and `Font.editorialHeadline` (260) have zero references outside their definitions (grep whole-word -> def only). editorialBody/editorialMeta/editorialSubMeta are still used; these two are not.
- **Fix:** Remove editorialDisplay (259) and editorialHeadline (260).
- **Evidence:** Substring grep: editorialDisplay only at DesignTokens.swift:259; editorialHeadline only at :260 — both def-only. Siblings still used: editorialBody -> SuggestionDetailSheet.swift:51,60; editorialMeta -> 8 call sites (DiscoveryPersonalizeSheet, DiscoveryRunSection, OnboardingFlowView); editorialSubMeta -> SuggestionDetailSheet.swift:42. Confirmed dead. Severity low correct.

### [LOW] deadcode — Unused Font.terracottaHeadlineItalic (and its only dependency appSerifItalic)
- **File:** `Views/Shared/DesignTokens.swift:275`
- **Issue:** `Font.terracottaHeadlineItalic` (line 275) is referenced nowhere outside its definition. It is the sole consumer of `Font.appSerifItalic(...)` (DesignTokens.swift:217), so removing it leaves appSerifItalic unused as well. The `appSansItalic` helper (line 201) is likewise referenced nowhere.
- **Fix:** Remove terracottaHeadlineItalic (275); then remove the now-unused appSerifItalic (217) and appSansItalic (201) helper funcs.
- **Evidence:** `terracottaHeadlineItalic` appears ONLY at DesignTokens.swift:275 (its def). `appSerifItalic` appears only at its def (DesignTokens.swift:217) and inside terracottaHeadlineItalic's def (:275) — so removing the latter leaves appSerifItalic unused. `appSansItalic` appears ONLY at its def (:201). All three are dead/transitively-dead as claimed. Severity low correct.

### [LOW] deadcode — Unused UIFont tokens appTerracottaHeadlineCompact, appEditorialHeadline, appEditorialSummary
- **File:** `Views/Shared/DesignTokens.swift:319`
- **Issue:** The UIFont computed tokens `appTerracottaHeadlineCompact` (319), `appEditorialHeadline` (323), and `appEditorialSummary` (327) are referenced nowhere outside their definitions (grep whole-word -> def only). Only `UIFont.appReaderBody` among the UIFont tokens has external usage.
- **Fix:** Remove appTerracottaHeadlineCompact (319), appEditorialHeadline (323), and appEditorialSummary (327) from the UIFont extension.
- **Evidence:** Whole-tree grep: appTerracottaHeadlineCompact only at DesignTokens.swift:319; appEditorialHeadline only at :323; appEditorialSummary only at :327 — all UIFont computed tokens, def-only. Contrast: UIFont.appReaderBody has real external usage (SelectableText.swift:21, SelectableMarkdownView.swift:23, ContentDetailView.swift:3039). The three flagged UIFont tokens are dead. Severity low correct.

### [LOW] deadcode — ArticleDetailViewModel.cancellables is declared but never used
- **File:** `ViewModels/ArticleDetailViewModel.swift:20`
- **Issue:** `private var cancellables = Set<AnyCancellable>()` (line 20) and the `import Combine` exist but nothing in the class ever stores into or reads `cancellables`; all data flow is async/await. This is dead state left over from a Combine-based design.
- **Fix:** Remove the `cancellables` property and the `import Combine` if no Combine usage remains.
- **Evidence:** ArticleDetailViewModel.swift line 20 declares `private var cancellables = Set<AnyCancellable>()`. Grep over the file shows `cancellables` appears ONLY at line 20 (the declaration); nothing ever stores into or reads it. `import Combine` (line 9) exists but all data flow is async/await (loadArticle uses `try await`). Dead Combine-era state. Severity low confirmed.

### [LOW] deadcode — Duplicated identical onChange condition
- **File:** `Views/Components/LongFormCardStackView.swift:52`
- **Issue:** Lines 52-54 and 55-61 both begin with `if oldCount == 0 && newCount > 0`. The first block sets currentIndex = 0; the second (separate, identical guard) logs and kicks off prefetch. They could be a single block; as written the condition is evaluated twice for no reason and slightly obscures intent.
- **Fix:** Merge the two `if oldCount == 0 && newCount > 0` blocks into one.
- **Evidence:** LongFormCardStackView.swift lines 52-54 `if oldCount == 0 && newCount > 0 { currentIndex = 0 }` and lines 55-61 `if oldCount == 0 && newCount > 0 { cardStackLogger.info(...); Task { ... } }` are two separate blocks guarded by the identical condition, evaluated twice. Real minor redundancy. Note dimension 'deadcode' is a slight mislabel (it is redundant-but-live code, not dead code), but the observation is accurate; low severity correct.

### [LOW] deadcode — topicsSection is never referenced
- **File:** `Views/Components/ArticleCardView.swift:188`
- **Issue:** The computed view `topicsSection` (lines 187-203) is defined but never used anywhere in the codebase (grep confirms the only occurrence is its definition). The `topics` parameter passed into ArticleCardView is consequently only consumed by dead UI plus the debug log in logPreviewState. This is dead code that also drags an otherwise-unused FlowLayout dependency and `.cornerRadius(12)` literal into the card.
- **Fix:** Remove topicsSection (and, if topics is not otherwise displayed, consider dropping the topics parameter or wiring topicsSection into cardContent if it was meant to be shown).
- **Evidence:** grep across newsly + ShareExtension (excluding build dirs) for `topicsSection` returns exactly one hit: ArticleCardView.swift:188 (its own definition). cardContent (lines 43-67) composes heroImageSection, titleSection, metadataRow, hookSection, keyPointsSection, Spacer, actionButtonsRow — topicsSection is never referenced. The `topics` param is still consumed by logPreviewState (line 395, `topics?.count`). Confirmed dead view. Minor overstatement in detail: FlowLayout is NOT dragged solely by this view (it is also used in StructuredSummaryView.swift and OnboardingFlowView.swift), so removing topicsSection would not drop the FlowLayout dependency. Core dead-code claim confirmed; low severity correct.

### [LOW] deadcode — categoryColor(for:) is never called
- **File:** `Views/Components/StructuredSummaryView.swift:299`
- **Issue:** The private method categoryColor(for:) (lines 299-310) is defined on StructuredSummaryView but never invoked; ModernKeyPointRow has its own categoryConfig(for:) which is the one actually used. Dead method.
- **Fix:** Delete categoryColor(for:).
- **Evidence:** categoryColor(for:) defined at StructuredSummaryView.swift:299-310 on StructuredSummaryView. Grep across newsly + ShareExtension (excluding build/dep dirs) returns exactly ONE hit: the definition itself (line 299). No call sites. ModernKeyPointRow.categoryConfig(for:) (line 322) is the helper actually used by the rendered rows (lines 337, 358). Dead method confirmed.

### [LOW] deadcode — onTopicDeepDive closure property is stored but never invoked
- **File:** `Views/Components/StructuredSummaryView.swift:23`
- **Issue:** var onTopicDeepDive: ((String) -> Void)? is declared (line 23) but never called anywhere in the view; topic deep-dive is handled instead via startTopicChat/topicSession. It is also not passed by the call site in ContentDetailView. Dead parameter.
- **Fix:** Remove onTopicDeepDive, or wire it up if the context-menu deep dive was intended to delegate to the parent.
- **Evidence:** var onTopicDeepDive: ((String) -> Void)? declared at StructuredSummaryView.swift:23. Grep for 'onTopicDeepDive' across the tree returns exactly ONE hit: the declaration (line 23) — never invoked. The only call site, ContentDetailView.swift:248-263, passes summary/contentId/startTopicSession but NOT onTopicDeepDive. Topic deep-dive is handled via startTopicChat/topicSession instead. Dead parameter confirmed.

### [LOW] deadcode — selectedTopic @State is written but never read
- **File:** `Views/Components/StructuredSummaryView.swift:29`
- **Issue:** @State private var selectedTopic is assigned in the topic pill context menu (line 260: selectedTopic = topic) but its value is never read anywhere. startTopicChat(topic:) uses the local `topic` argument, not selectedTopic. The state and its assignment are dead.
- **Fix:** Remove the selectedTopic @State and the line that assigns it.
- **Evidence:** @State private var selectedTopic: String? at StructuredSummaryView.swift:29; assigned at line 260 (selectedTopic = topic) inside the topic-pill context menu, immediately followed by startTopicChat(topic: topic) which uses the local arg. Within this file selectedTopic is only declared (29) and written (260) — never read. Other grep hits (ChatSessionDetail.swift, ContentDetailView.swift, ChatNavigationCoordinator.swift) are unrelated struct properties/init labels, not this @State. Written-but-never-read confirmed.

### [LOW] deadcode — buildImageURL duplicated across at least five files
- **File:** `Views/Components/ContentCard.swift:128`
- **Issue:** The identical buildImageURL(from:) helper is copy-pasted in ContentCard.swift (128), ArticleCardView.swift (380), ContentDetailView.swift (3055), LongFormCard.swift (318), and ContentImagePrefetcher.swift (38). Divergent copies are a maintenance hazard (a base-URL fix must be made in five places).
- **Fix:** Extract a single shared helper (e.g. a static on AppSettings or a URL helper utility) and call it from all sites.
- **Evidence:** buildImageURL(from:) is defined as a private helper in 5 files: ContentCard.swift:128, ArticleCardView.swift:380, ContentDetailView.swift:3055, LongFormCard.swift:318, ContentImagePrefetcher.swift:38. Read of ContentCard:128-139 and ArticleCardView:380-390 shows byte-for-byte identical logic (http(s) passthrough, else AppSettings.shared.baseURL string concatenation). A base-URL fix would need to be made in 5 places. Duplication hazard confirmed.

### [LOW] deadcode — Entire ChatGPTDeepLink utility is unused
- **File:** `Services/ChatGPTDeepLinkService.swift:6`
- **Issue:** `ChatGPTDeepLink` and both static methods `openPreferApp(fallbackWebURL:completion:)` and `openUniversalOrWeb(_:)` have zero references across newsly, ShareExtension, newslyTests, and newslyUITests. The whole file is dead code.
- **Fix:** Delete Services/ChatGPTDeepLinkService.swift (or wire it up if ChatGPT deep-linking is still intended).
- **Evidence:** grep -rn 'ChatGPTDeepLink' across newsly + ShareefExtension returns only ChatGPTDeepLinkService.swift:6 (the class definition). grep for 'openPreferApp|openUniversalOrWeb' returns only their own definitions at lines 15 and 32. No callers anywhere. Entire file is dead code as claimed.

### [LOW] deadcode — ChatService.sendMessage(sessionId:message:onProcessingStarted:) is unused
- **File:** `Services/ChatService.swift:278`
- **Issue:** The 3-arg blocking `sendMessage` (lines 278-293) has no production or test callers; the only `sendMessage(` calls in the app are ChatSessionViewModel's own `sendMessage(text:)`. The ViewModel drives sending via `sendMessageAsync` + its own `pollUntilComplete`. This method (and the duplicated polling logic it relies on) is dead.
- **Fix:** Remove the unused `sendMessage(sessionId:message:onProcessingStarted:)` method.
- **Evidence:** ChatService.swift line 278 defines the 3-arg `sendMessage(sessionId:message:onProcessingStarted:)`. It is NOT a protocol requirement — ChatSessionServicing (lines 43-52) declares only sendMessageAsync, not sendMessage. grep for 'sendMessage' shows every call site is either ChatSessionViewModel's own `sendMessage(text:)` (lines 114/189/282/338/360/951) or `sendMessageAsync` (ChatService 211/284, ContentDetailView 613/740, ViewModel 311, tests). No caller invokes the 3-arg overload defined at line 278. Dead as claimed.

### [LOW] deadcode — ChatService.getInitialSuggestions is unused in production
- **File:** `Services/ChatService.swift:296`
- **Issue:** `getInitialSuggestions(sessionId:)` is declared in the `ChatSessionServicing` protocol and implemented here, but the only references are the protocol declaration and a test mock in newslyTests/ChatSessionViewModelTests.swift:546. No production code calls it, and the InitialSuggestionsResponse model exists solely for it. The constructed ChatMessage also omits sourceMessageId/displayKey.
- **Fix:** Remove `getInitialSuggestions` from both the protocol and ChatService (and the now-orphan InitialSuggestionsResponse) unless a caller is planned.
- **Evidence:** grep across newsly/ShareExtension/newslyTests shows getInitialSuggestions only at: protocol decl ChatService.swift:47, impl ChatService.swift:296, and test mock ChatSessionViewModelTests.swift:546. No '.getInitialSuggestions(' call site anywhere (grep empty). Test testEmptyContextualSessionDoesNotAutoGenerateInitialSuggestions (line 361) explicitly asserts initialSuggestionsCallCount==0 (line 383), i.e. it is verified to NEVER be called. InitialSuggestionsResponse (Models/ChatSessionDetail.swift:80) is referenced only by this method at ChatService.swift:299. The constructed ChatMessage (lines 304-309) passes only id/role/timestamp/content, omitting sourceMessageId/displayKey. Dead in production confirmed.

### [LOW] deadcode — ChatService.startAdHocChat is unused
- **File:** `Services/ChatService.swift:405`
- **Issue:** `startAdHocChat(initialMessage:provider:)` has no callers anywhere in newsly, ShareExtension, or the test targets.
- **Fix:** Remove `startAdHocChat` unless an ad-hoc chat entry point is planned.
- **Evidence:** grep for startAdHocChat across newsly/ShareExtension/newslyTests returns only its own definition at ChatService.swift:405. It is NOT in the ChatSessionServicing protocol (protocol only lists getInitialSuggestions). No '.startAdHocChat(' call site exists. createSession(initialMessage:) is only invoked from inside startAdHocChat (line 411). Confirmed dead.

### [LOW] deadcode — OnboardingService.buildProfile / fastDiscover / parseVoice are never called
- **File:** `Services/OnboardingService.swift:23`
- **Issue:** buildProfile (line 23), fastDiscover (line 32), and parseVoice (line 97) have no call sites anywhere in the app; only audioDiscover, discoveryStatus, complete, and markTutorialComplete are used. This also makes the request/response models OnboardingProfileRequest, OnboardingProfileResponse, OnboardingFastDiscoverRequest, OnboardingVoiceParseRequest, and OnboardingVoiceParseResponse dead. The current onboarding flow uses the async audioDiscover + polling path instead.
- **Fix:** Delete the three unused service methods and their now-orphaned request/response models in Models/Onboarding.swift, after confirming no out-of-tree consumers (e.g. tests) depend on them.
- **Evidence:** OnboardingService.swift: buildProfile (L23), fastDiscover (L32), parseVoice (L97). Grep across newsly+ShareExtension (excluding build dirs): each method name appears ONLY at its own definition line — no call sites (`.buildProfile`/`.fastDiscover`/`.parseVoice` return only the def lines). Models: OnboardingProfileRequest/Response each appear only at Onboarding.swift def + the dead buildProfile signature; OnboardingFastDiscoverRequest only at def + dead fastDiscover signature; OnboardingVoiceParseRequest/Response only at def + dead parseVoice. (Note: OnboardingFastDiscoverResponse is correctly NOT claimed dead — it IS used in DiscoveryPersonalizeViewModel/OnboardingViewModel/OnboardingStateStore.) Real dead code; low severity appropriate.

### [LOW] deadcode — advanceToChoice() and OnboardingStep.intro are unreachable
- **File:** `ViewModels/OnboardingViewModel.swift:191`
- **Issue:** advanceToChoice() (line 191) is never called. OnboardingStep.intro (case 0) is never assigned: step defaults to .choice and no code path sets .intro, so the `case .intro` branches in OnboardingFlowView.content (line 58) and currentStepInfo (line 992) are dead. This is harmless but adds confusion about the state machine's entry point.
- **Fix:** Remove advanceToChoice() if no flow needs it, and either remove the .intro case or document why it is retained (e.g. persisted-snapshot back-compat). If kept for Codable back-compat of OnboardingStep raw values, leave a comment.
- **Evidence:** OnboardingViewModel.swift L191 `func advanceToChoice() { step = .choice }` — grep shows the symbol appears ONLY at L191, no callers. `.intro` (enum case at L20) is referenced only in two switch branches (OnboardingFlowView L58, L992) and never assigned: grep for `= .intro`/`=.intro` returns ZERO hits, `@Published var step: OnboardingStep = .choice` (L97) is the default, and all 9 `step = ...` assignments target other cases. Snapshot restore (L686) cannot produce `.intro` because it is never persisted. Unreachable case + dead method; low severity correct.

### [LOW] deadcode — startInterestingUnreadNewsChat() is never called
- **File:** `ViewModels/KnowledgeHubViewModel.swift:178`
- **Issue:** KnowledgeHubViewModel.startInterestingUnreadNewsChat() (and its dependency InterestingUnreadNewsAssistantAction) has no call sites anywhere in newsly/ or ShareExtension/. The KnowledgeView hub actions only invoke startSummaryChat/startCommentsChat/startFindArticlesChat/startFindFeedsChat. Dead method.
- **Fix:** Remove startInterestingUnreadNewsChat() if no screen plans to use it, or wire it into a hub action.
- **Evidence:** KnowledgeHubViewModel.swift:178 `func startInterestingUnreadNewsChat()` — grep across newsly/ + ShareExtension/ for `startInterestingUnreadNewsChat` returns exactly one hit: its own definition. The KnowledgeView hub only wires startSummaryChat/startCommentsChat/startFindArticlesChat/startFindFeedsChat. The method is dead. Nuance: the parenthetical claim that `InterestingUnreadNewsAssistantAction` is also dead is WRONG — that enum is used in ShortFormView.swift:329-330 (and defined ChatSessionDetail.swift:144). But the core finding (the VM method has no call sites) is correct.

### [LOW] deadcode — fetchSuggestions() and DiscoveryViewModel.apply(DiscoverySuggestionsResponse) are unused
- **File:** `Services/DiscoveryService.swift:22`
- **Issue:** DiscoveryService.fetchSuggestions() has no callers; DiscoveryViewModel only uses fetchHistory(). Correspondingly DiscoveryViewModel.apply(_ response: DiscoverySuggestionsResponse) (line 233) and the DiscoverySuggestionsResponse model path are dead. DiscoverySuggestion.subscribeLabel and DiscoverySuggestion.displaySubtitle also have no call sites.
- **Fix:** Remove fetchSuggestions(), the unused apply(DiscoverySuggestionsResponse) overload, subscribeLabel, and displaySubtitle, or document why they are retained.
- **Evidence:** DiscoveryService.swift:22 `fetchSuggestions()` — grep returns only the definition, no callers. DiscoveryViewModel.swift:233 `private func apply(_ response: DiscoverySuggestionsResponse)` — the only `apply(...)` call in the VM is line 93 `apply(history)` which is the DiscoveryHistoryResponse overload (line 243); the SuggestionsResponse overload is never invoked. DiscoverySuggestionsResponse (model) is only referenced by these two dead methods plus the unused endpoint const APIEndpoints.swift:108. DiscoverySuggestion.subscribeLabel (DiscoverySuggestion.swift:201) — grep returns only its definition. DiscoverySuggestion.displaySubtitle (line 169) — the two `.displaySubtitle` view usages (ChatSessionHistoryView:29, ChatSessionCard:107) are on `session` (ChatSessionSummary.displaySubtitle), not a DiscoverySuggestion; the suggestion cards (DiscoverySuggestionCard:21, SuggestionDetailSheet:33) use `displayTitle`. All cited symbols are dead.

### [LOW] deadcode — markAllAsRead() VM method is never called
- **File:** `ViewModels/ContentListViewModel.swift:241`
- **Issue:** Grep for `viewModel.markAllAsRead` / `.markAllAsRead()` (the VM method, distinct from ContentService.markAllAsRead) returns no call sites. The ~60-line method (bulkMarkAsRead + per-type count decrement) is dead.
- **Fix:** Remove markAllAsRead() unless a caller is planned; if kept, add the missing per-type fallthrough handling.
- **Evidence:** ContentListViewModel.swift:241 `func markAllAsRead() async`. Grep for the VM method invocation (`.markAllAsRead` / `markAllAsRead(`) across newsly/ + ShareExtension/ returns only three hits: the VM definition (241), ContentService.markAllAsRead definition (ContentService.swift:453), and SettingsView.swift:674 which calls `ContentService.shared.markAllAsRead(contentType:)` — the SERVICE method, not the VM method. No call site invokes the ContentListViewModel instance method. It is dead.

### [LOW] deadcode — DownloadMoreMenu component is unused
- **File:** `Views/Components/DownloadMoreMenu.swift:8`
- **Issue:** `DownloadMoreMenu` has no call sites anywhere in the app (grep for `DownloadMoreMenu` returns only the definition). It is an orphaned component.
- **Fix:** Delete DownloadMoreMenu.swift, or adopt it where the 'Load more' button is currently hand-rolled (e.g. KnowledgeLibraryView).
- **Evidence:** grep -rn 'DownloadMoreMenu' across newsly + ShareExtension (excluding build/dep dirs) returns ONLY DownloadMoreMenu.swift:2 (file-header comment) and :8 (struct definition). No call sites anywhere. Orphaned component confirmed dead. Severity low accurate.

### [LOW] swiftui — Extremely large monolithic view body / file (3200+ lines) with heavy inline closures recomputed on every drag frame
- **File:** `Views/ContentDetailView.swift:133`
- **Issue:** `ContentDetailView` is a single ~3250-line struct whose `body` inlines the hero, all summary variants, discussion sheet, links sections, comment-tree indexing, and share/chat plumbing. The top-level `body` is wrapped with `.offset(x: dragAmount)` + `.animation(value: dragAmount)`, so the entire tree is re-evaluated on every swipe frame. Inside body, `buildDiscussionCommentIndex` (an O(n) tree build with recursive descendant counting) is called inline in `discussionSheet` (lines 2135 and 2165) and re-runs whenever that sub-body recomputes. Decomposing into child views with stable identity would let SwiftUI skip recomputation of unaffected subtrees during drag.
- **Fix:** Extract the hero, each summary section, and the discussion sheet into dedicated child views/components so drag-driven offset changes don't force re-evaluation of the whole tree; precompute the discussion comment index once (e.g. on payload assignment) rather than inside the view body.
- **Evidence:** Partly correct but central perf claim is overstated. CONFIRMED facts: file is 3247 lines (wc -l), top-level body has `.offset(x: dragAmount)` + `.animation(.interactiveSpring(...), value: dragAmount)` at lines 415-416, and buildDiscussionCommentIndex (line 2267) is an uncached O(n) tree build called inline at lines 2135 and 2165. WRONG claim: buildDiscussionCommentIndex is inside `discussionSheet`, which is presented via `.sheet(item: $activeSheet)` at lines 491-515 — a SEPARATE presentation context, NOT inline in the main drag tree. So it does NOT re-run on swipe frames as claimed; it only runs when the discussion sheet body recomputes (while the sheet is presented, not during the back-tree drag). The monolithic-file + decomposition recommendation is valid, but the 'recomputed on every drag frame' justification conflates the sheet body with the drag tree. Downgrading to low.

### [LOW] swiftui — Share presentation chained through DispatchQueue.main.async + onDismiss is fragile timing-based plumbing
- **File:** `Views/ContentDetailView.swift:1758`
- **Issue:** `queueShareContent` clears `activeSheet`, then `presentPendingShareIfNeeded` (invoked from the sheet `onDismiss`) calls `DispatchQueue.main.async { viewModel.shareContent(...) }`, which in turn retries presentation up to 8 times with 0.08s `asyncAfter` delays (ViewModel `presentActivityViewControllerWhenReady`). This timing-based UIKit presentation racing is brittle: if the dismiss animation is slow or another sheet is queued, the share sheet can fail to present silently (all 8 retries exhausted with no user feedback). The `.tweet` re-presentation similarly hard-codes a 0.3s delay (line 1736).
- **Fix:** Drive presentation off explicit completion (e.g. present from within the SwiftUI `.sheet`/`.fullScreenCover` lifecycle, or use the sheet's `onDismiss` completion) rather than polling for `isBeingDismissed`; surface a user-visible error if presentation ultimately fails.
- **Evidence:** queueShareContent (lines 1749-1752) sets pendingShareOption then clears activeSheet. presentPendingShareIfNeeded (lines 1754-1761) wraps `viewModel.shareContent(option:)` in `DispatchQueue.main.async`. shareContent -> presentActivityViewControllerWhenReady (ContentDetailViewModel.swift lines 763-791): maxAttempts=8 (line 767), retryDelaySeconds=0.08 (line 768), and on isBeingDismissed/isBeingPresented it `guard attempt < maxAttempts else { return }` (line 780) — silently returns with NO user feedback once 8 retries exhaust, then re-schedules via asyncAfter(0.08) (line 781). The .tweet path hard-codes a 0.3s delay: `DispatchQueue.main.asyncAfter(deadline: .now() + 0.3) { activeSheet = .tweet }` (lines 1736-1738). Timing-based UIKit presentation racing exactly as described; low severity appropriate.

### [LOW] swiftui — Prefetch loop is serial and ignores cancellation, blocking later detail fetches
- **File:** `ViewModels/CardStackKeyPointsLoader.swift:57`
- **Issue:** prefetch(contentIds:aroundIndex:) awaits fetchContentDetail one id at a time in a for-loop (line 67) with no Task.isCancelled checks. Because it is invoked from .task(id: currentIndex) in LongFormCardStackView, each index change cancels the prior task — but a serial fetch already in flight will run to completion (await is not auto-cancelled mid-request without cooperative checks), and remaining ids in the loop are only skipped when the parent task is cancelled (which SwiftUI does, so the loop continuation may stop, but a slow first fetch still delays the user-visible current card). Fetching the visible card first / in parallel would be more responsive.
- **Fix:** Prioritize the current index first, optionally fan out with a TaskGroup, and add `guard !Task.isCancelled else { return }` between iterations.
- **Evidence:** CardStackKeyPointsLoader.swift:57 begins `for index in indicesToFetch {` and line 67 `let detail = try await contentService.fetchContentDetail(id: contentId)` awaits one id at a time; no Task.isCancelled checks anywhere in prefetch(). Invoked from LongFormCardStackView.swift:63 `.task(id: currentIndex)` via prefetchKeyPoints (line 309-315). indicesToFetch = [currentIndex-1, currentIndex, currentIndex+1, currentIndex+2] (lines 50-55), so the visible card (currentIndex) is fetched 2nd, after the previous index. Accurate responsiveness concern, not a correctness bug. Low severity appropriate.

### [LOW] swiftui — Monolithic 600+ line view with heavy per-render computed work in body
- **File:** `Views/LongFormView.swift:40`
- **Issue:** LongFormView.body and its surrounding computed properties (bootstrap state, source progress, audio state) form a very large single view. body computes `items` and `lastItemId` each render, and numerous computed vars (longFormSources sorts+filters configs on every access at line 423-427, totalProcessedSourceItems/totalSourceItemsProcessing reduce on every access) are recomputed during view evaluation. The bootstrap subtree alone re-sorts sources repeatedly. This is recomputation-in-body that should be memoized or decomposed into child views so SwiftUI can scope invalidation.
- **Fix:** Extract the bootstrap state and source-progress UI into dedicated subviews and cache the sorted/filtered source list (e.g. compute once in the view model) instead of re-deriving it in multiple computed properties per render.
- **Evidence:** LongFormView.swift is 667 lines (wc -l). longFormSources (line 423) does .filter{isActive}.sorted(by: compareSources) on every access; totalProcessedSourceItems (445) and totalSourceItemsProcessing (451) each .reduce over longFormSources on every access. grep shows these computed vars are referenced ~15 times across the body/computed props (lines 408,438,442,446,451,458,468,469,472,478,482,490,505,529,531,538). body also computes `items` and `lastItemId` each render (lines 41-42). Real recomputation-in-body / decomposition concern; low severity reasonable.

### [LOW] swiftui — FlowLayout recomputes subview sizeThatFits redundantly
- **File:** `Views/Components/StructuredSummaryView.swift:479`
- **Issue:** FlowResult computes subview.sizeThatFits(.unspecified) once in the main init loop (line 440) and then again for every subview inside finalizeRow (line 479). For the placeSubviews pass FlowResult is built a second time as well (lines 405-409), so each topic pill's sizeThatFits is measured several times per layout pass. Harmless for a handful of topic pills but unnecessary work and an O(n) duplication baked into the layout.
- **Fix:** Cache the idealSize per subview (e.g. store sizes in the row alongside frames) instead of re-querying sizeThatFits in finalizeRow, and consider using the Layout cache parameter to avoid rebuilding FlowResult in placeSubviews.
- **Evidence:** In FlowResult.init each subview is measured at StructuredSummaryView.swift:440 (subview.sizeThatFits(.unspecified)) during the row-packing loop, then measured AGAIN at line 479 inside finalizeRow (subviews[index].sizeThatFits(.unspecified)) when building frames. placeSubviews (lines 405-410) constructs a second FlowResult, so the whole double-measure runs again per layout pass. Redundant O(n) re-measurement confirmed; harmless for a few pills but real duplicated work. 'low' is appropriate.

### [LOW] swiftui — Custom global-coordinate DragGesture can fight system pop and interactive keyboard dismiss
- **File:** `Views/ChatSessionView.swift:147`
- **Issue:** edgeBackSwipeGesture is a DragGesture(minimumDistance: 20, coordinateSpace: .global) attached to a 28pt leading overlay (edgeBackSwipeZone) while the view also sets .navigationBarBackButtonHidden(true) and .scrollDismissesKeyboard(.interactively). A manual horizontal drag recognizer overlapping the system interactive back-swipe region and the interactive keyboard-dismiss gesture is a common source of gesture-arbitration jank (swipe sometimes triggers scroll/keyboard instead of back, or vice versa). The .offset(x:) is also applied to the whole message-list chain, so the safeAreaInset dock and overlays slide together, which is the intended look but couples gesture state to the entire screen.
- **Fix:** Prefer enabling the system interactive-pop (custom back button without hiding the gesture) or scope the custom recognizer with .highPriorityGesture/.simultaneousGesture and a tighter activation so it does not race the scroll/keyboard gestures. Verify on device that mid-list horizontal drags still scroll and that keyboard dismiss is unaffected.
- **Evidence:** ChatSessionView.swift line 148: `DragGesture(minimumDistance: 20, coordinateSpace: .global)` attached via edgeBackSwipeZone (28pt leading overlay, line 139-145 width=ChatSessionDesign.edgeBackSwipeWidth). Same view sets `.navigationBarBackButtonHidden(true)` (line 62) and `.scrollDismissesKeyboard(.interactively)` (line 61), and `.offset(x: edgeBackDragOffset)` (line 54) is applied to the whole messageListView+safeAreaInset chain. A manual global-coordinate horizontal drag overlapping system back-swipe + interactive keyboard dismiss is a genuine gesture-arbitration concern, exactly as described. Low severity appropriate.

### [LOW] swiftui — Initial scroll-to-bottom depends on onChange that may not fire on first render
- **File:** `Views/Chat/ChatMessageList.swift:101`
- **Issue:** Initial bottom anchoring is gated entirely by .onChange(of: timeline.last?.id) flipping hasAnchoredInitialScroll. onChange does not fire for the value present at first render. The current flow happens to work because loadSession() populates the timeline asynchronously after the view appears (changing last?.id). However, if the view is ever created with an already-populated timeline (route carrying a session whose messages are present synchronously, or a future cache/prefetch path), the first frame will not scroll to bottom until the last id next changes, leaving the user scrolled to the top of a long transcript. There is no .task/.onAppear that performs an initial proxy.scrollTo.
- **Fix:** Add an explicit initial anchor in .onAppear/.task (when hasMessages and !hasAnchoredInitialScroll, scrollTo timeline.last?.id with anchor .bottom and no animation), so initial positioning does not depend on a subsequent id change.
- **Evidence:** ChatMessageList.swift line 101 `.onChange(of: timeline.last?.id)` is the sole gate for initial anchoring (lines 103-106 flip hasAnchoredInitialScroll then proxy.scrollTo). grep for onAppear/.task/scrollTo in the file shows no .task or .onAppear performing an initial scrollTo (scrollTo only appears inside onChange handlers at 105/110/119/126 and the jump-to-latest button at 178). onChange does not fire for the value present at first render, so a synchronously-populated timeline would not anchor to bottom. Accurately described; low severity appropriate.

### [LOW] swiftui — Debounce Task captures self strongly without [weak self]
- **File:** `ViewModels/TweetSuggestionsViewModel.swift:92`
- **Issue:** `creativityDebounceTask = Task { ... lastCreativity = newValue; await regenerate() }` captures `self` strongly. It is short-lived (500ms) and cancelled on the next slider change, so it is not a durable leak, but it does keep the view model (and through it the sheet's StateObject) alive for the debounce window even if the sheet is dismissed mid-slide. Minor; flagged for consistency with the rest of the codebase which uses [weak self] in tasks.
- **Fix:** Use `Task { [weak self] in guard let self else { return } ... }` for the debounce task, matching the pattern used elsewhere.
- **Evidence:** TweetSuggestionsViewModel.swift:92: creativityDebounceTask = Task { ... lastCreativity = newValue; await regenerate() } with no [weak self]; the closure references self via lastCreativity/regenerate so self is captured strongly. Class is @MainActor final class TweetSuggestionsViewModel: ObservableObject (lines 14-15). Task is the only Task{} in the file and is cancelled on next slider change (line 89) + 500ms sleep, so not a durable leak. Note the finding's 'rest of codebase uses [weak self]' is uneven: this file has 0 weak-self uses while ChatSessionViewModel has 13. The strong capture is real and self-characterized as minor; low severity is correct.

### [LOW] swiftui — TimelineView at 30fps rebuilds the entire title section (mascot image included) for a 3pt offset
- **File:** `Views/LandingView.swift:47`
- **Issue:** titleSection wraps the whole VStack (Mascot image with two shadows, display text with two glow shadows, subtitle) inside a TimelineView(.animation(minimumInterval: 1/30)). Every frame the closure recomputes and the full subtree is re-evaluated just to apply a ~3pt vertical offset and a cycling glow color. The static Mascot/title/subtitle content does not need to be rebuilt 30x/sec.
- **Fix:** Hoist the static content out of the TimelineView and apply only the animated .offset(y:) and .shadow(color:) via the timeline-derived values, or drive the subtle motion with a single .modifier so SwiftUI only re-applies the changing geometry rather than rebuilding the image/text subtree.
- **Evidence:** LandingView.swift L47-78: `titleSection` wraps the ENTIRE VStack (Image("Mascot") .frame(220x220) at L53, the 'Newsbuddy' title with two glow shadows L68-69, subtitle L71) inside `TimelineView(.animation(minimumInterval: 1.0/30.0))`. Closure recomputes yOffset (L49) and glowColor (L50) each frame and applies `.offset(y: yOffset)` (L77). WatercolorBackground.titleOscillation (L139) = sin(t*0.18)*3.0 (±3pt, confirmed by L137 comment). The static mascot/title/subtitle subtree is re-evaluated 30x/sec for a 3pt offset + cycling glow. Real minor SwiftUI perf concern; low severity appropriate.

### [LOW] swiftui — Pulse animation driven by onAppear/onDisappear of a conditional view; pulseScale not reset before reuse
- **File:** `Views/Onboarding/OnboardingMicButton.swift:33`
- **Issue:** The recording pulse ring is conditionally inserted (audioState == .recording) and starts a repeatForever animation in .onAppear, resetting pulseScale to 1.0 only in .onDisappear. Because the ring view is added/removed by state rather than always present, the repeatForever animation lifecycle is coupled to view identity; if the ring re-appears before the prior onDisappear resets state, or under reduce-motion (no guard here, unlike FinalizingSparkle which does guard reduceMotion), the pulse keeps animating. Minor: reduce-motion is not respected for this infinite pulse.
- **Fix:** Guard the pulse animation with @Environment(\.accessibilityReduceMotion) as FinalizingSparkle does, and consider keeping the ring always present with opacity 0 when not recording so the animation lifecycle is deterministic.
- **Evidence:** OnboardingMicButton.swift L27-39: pulse-ring Circle is conditionally inserted only when `audioState == .recording`; `.onAppear` (L33-37) starts `.easeInOut(duration:1.2).repeatForever(autoreverses:true)` on pulseScale; `.onDisappear` (L38) resets pulseScale=1.0 — lifecycle coupled to view identity. No reduceMotion in this file (grep of Views/Onboarding shows AncientScrollRevealView L17 and FinalizingSparkle L1095 have @Environment(\.accessibilityReduceMotion), and FinalizingSparkle guards `guard !reduceMotion else { return }` at OnboardingFlowView L1111 — OnboardingMicButton has none). Inconsistency and infinite pulse ignoring reduce-motion are real; low severity correct.

### [LOW] swiftui — @StateObject initialized from a shared singleton (should be @ObservedObject)
- **File:** `Views/MoreView.swift:10`
- **Issue:** processingCountService is declared @StateObject private var processingCountService = ProcessingCountService.shared. @StateObject is meant to own a view-created instance; assigning a long-lived singleton expresses ObservedObject semantics through StateObject. It happens to work because the autoclosure runs once and returns the existing shared instance, but it is misleading and, if this view is ever re-instantiated, masks ownership intent. The same pattern appears in ProcessingStatsView.swift lines 11-12. (Note: this is a repo-wide convention, see ContentView.swift / ShortFormView.swift, so it is low priority — but it is technically an anti-pattern.)
- **Fix:** Use @ObservedObject for references to shared singletons (e.g. @ObservedObject private var processingCountService = ProcessingCountService.shared), reserving @StateObject for objects the view itself creates and owns (such as the fresh ScraperSettingsViewModel in ProcessingStatsView line 13, which is correctly @StateObject).
- **Evidence:** MoreView.swift:10 is '@StateObject private var processingCountService = ProcessingCountService.shared'. ProcessingStatsView.swift:11-12 confirmed: '@StateObject private var processingCountService = ProcessingCountService.shared' / '@StateObject private var unreadCountService = UnreadCountService.shared'. The same @StateObject-from-singleton pattern is repo-wide: ContentView.swift:14,17,18; ShortFormView.swift:17,18; LongFormView.swift:20,21; ContentDetailView.swift:79,96. It is technically an anti-pattern (StateObject is for view-owned instances) but is an established convention, so low priority is accurate.

### [LOW] swiftui — Data loading triggered from onAppear instead of .task; runs again on every re-appear
- **File:** `Views/KnowledgeDiscoveryView.swift:26`
- **Issue:** The view loads suggestions inside .onAppear { Task { await viewModel.loadSuggestions() } }. onAppear fires on every appearance (e.g., returning from a pushed view or tab switch) and spawns an unstructured Task that is not tied to the view lifecycle, so it is not cancelled if the view disappears mid-flight. Combined with the missing re-entrancy guard in the VM (see related finding), this is the standard onAppear-vs-.task anti-pattern.
- **Fix:** Use `.task { await viewModel.loadSuggestions() }` so the load is lifecycle-scoped and auto-cancelled, and rely on hasLoaded to dedupe.
- **Evidence:** KnowledgeDiscoveryView.swift:26-28 is '.onAppear { Task { await viewModel.loadSuggestions() } }'. This is the onAppear-vs-.task anti-pattern: onAppear fires on every appearance and spawns an unstructured Task not tied to view lifecycle (not cancelled on disappear). Combined with the missing re-entrancy guard in finding 121. Latent: KnowledgeDiscoveryView has no external references (grep shows only its own definition). Low severity correct.

### [LOW] swiftui — scrollToFocusRequest uses DispatchQueue.main.async + manual animation instead of view-driven scroll
- **File:** `Views/KnowledgeView.swift:310`
- **Issue:** scrollToFocusRequest is invoked from both onAppear and onChange(of: focusRequest) and hops through DispatchQueue.main.async to call proxy.scrollTo and also flips showNarrationList = true and onFocusHandled?(request). Dispatching state mutations (showNarrationList, the focus-handled callback) out of the SwiftUI update via a bare async hop is fragile: the proxy capture is fine, but bundling a sheet-presentation side effect with the scroll inside an unmanaged async block can present the narration sheet at unexpected times and double-fire if focusRequest changes rapidly. There is also no guard that the request was not already handled.
- **Fix:** Drive the scroll/animation directly in the onChange/onAppear closure (withAnimation) without the async hop where possible, and gate the showNarrationList side effect so it only triggers once per unique request id.
- **Evidence:** scrollToFocusRequest(_:proxy:) at KnowledgeView.swift:304-319 wraps everything in DispatchQueue.main.async (line 310): inside it calls proxy.scrollTo (312), sets showNarrationList=true (315) and calls onFocusHandled?(request) (317). It is invoked from onAppear (line 98) and onChange(of: focusRequest) (lines 100-101). Bundling sheet-presentation side effects (showNarrationList) and the focus-handled callback in a bare async hop, with no guard that the request was already handled, is the described fragility. Low severity appropriate (state mutations are not in a tight loop).

### [LOW] swiftui — Voice recording state has two competing sources of truth (explicit sets vs onStateChange callback)
- **File:** `ViewModels/KnowledgeHubViewModel.swift:349`
- **Issue:** isVoiceRecording/isVoiceTranscribing are set both explicitly inside startVoiceRecording/stopVoiceRecordingAndStartChat/onStopReason AND reactively inside the onStateChange callback (lines 349-362). Because the transcription service can emit onStateChange asynchronously, these two paths can fight: e.g. after onStopReason(.silenceAutoStop) sets isVoiceActionInFlight = true and submits, a late onStateChange(.idle) could clear isVoiceRecording/isVoiceTranscribing independently of the in-flight submit, producing inconsistent button enable/busy states. The same dual-source pattern exists in LearningDeckFocusRecorder.configureTranscriptionCallbacks.
- **Fix:** Pick one source of truth: either derive isVoiceRecording/isVoiceTranscribing solely from onStateChange, or remove the onStateChange handler and manage state explicitly. Avoid setting the same flags from both.
- **Evidence:** KnowledgeHubViewModel.swift: isVoiceRecording/isVoiceTranscribing are set explicitly in startVoiceRecording (lines 271-276), stopVoiceRecordingAndStartChat (288-294), and onStopReason (324-339), AND reactively in onStateChange (349-362). Two competing sources of truth as described; a late onStateChange(.idle) can clear flags out of sync with an in-flight submit. Secondary claim also confirmed: LearningDeckFocusRecorder (LearningDeckCreateSheet.swift:204) has the identical dual-source pattern -- explicit sets in startRecording (262-267), stopRecording (279-287), onStopReason (313-321) AND reactive onStateChange (331-344). Low severity appropriate (race is timing-dependent and recoverable).

### [LOW] swiftui — isLoading @State is written but never read (dead state)
- **File:** `Views/Components/CachedAsyncImage.swift:21`
- **Issue:** `@State private var isLoading` is set true/false throughout loadImage() but the body (38-51) only branches on loadedImage/thumbnailImage; isLoading never gates any view, so each assignment is a no-op state mutation. It is effectively dead and slightly misleading.
- **Fix:** Remove isLoading, or surface it (e.g. drive the placeholder/spinner) if a loading indicator was intended.
- **Evidence:** CachedAsyncImage.swift: `@State private var isLoading = false` (line 21) is written at lines 67, 72, 82, 107, 112 but NEVER read. grep for isLoading shows only the declaration + 5 assignments. body (lines 38-51) branches only on `loadedImage`/`thumbnailImage`; isLoading gates no view. Dead/no-op state confirmed. Severity low is correct.

### [LOW] swiftui — unseenCount relies on a manual objectWillChange.send() because state lives in UserDefaults
- **File:** `ViewModels/SubmissionStatusViewModel.swift:81`
- **Issue:** `lastViewedSubmissionCreatedAt` is read from UserDefaults (not @Published), so `unseenCount` (consumed by the More-tab badge in ContentView) does not invalidate automatically. `markCurrentSubmissionsViewed()` compensates with a manual `objectWillChange.send()`. This is fragile: any future code path that writes the defaults key without calling send() will leave the badge stale, and the badge can only refresh while SubmissionsView is on-screen (it is the only caller of markCurrentSubmissionsViewed).
- **Fix:** Promote the last-viewed timestamp to an `@Published` property (still backed by UserDefaults via didSet) so SwiftUI tracks it directly and the manual objectWillChange.send() becomes unnecessary.
- **Evidence:** SubmissionStatusViewModel.swift: `lastViewedSubmissionCreatedAt` is a computed property reading UserDefaults (lines 84-88), not @Published. `unseenCount` (line 30) depends on it and on `submissions` (@Published). markCurrentSubmissionsViewed (line 72) writes the defaults key (line 80) then manually calls `objectWillChange.send()` (line 81) to force badge refresh — fragile compensation. Any future writer of StorageKey.lastViewedSubmissionCreatedAt without send() leaves the badge stale. Accurate. Severity low correct.

### [LOW] token — Floating back button scrim uses raw Color(red:green:blue:) instead of a token
- **File:** `Views/ContentDetailView.swift:1296`
- **Issue:** floatingBackButton fills its circular background with Color(red: 0.07, green: 0.06, blue: 0.05).opacity(0.42) and casts a shadow with Color(red: 0.05, green: 0.045, blue: 0.04).opacity(0.28) (line 1303). These are hand-built near-black tints for a scrim/shadow over hero imagery, constructed outside DesignTokens.swift/ReaderPalette.swift. While the intent (a dark translucent scrim for legibility over photos) is reasonable, the raw RGB construction bypasses the token system; a scrim/overlay token (or Color.black.opacity for a true neutral) would keep content color centralized.
- **Fix:** Replace with a neutral scrim derived from a token (e.g. Color.black.opacity(...) for the scrim and shadow, or add a dedicated overlayScrim token in DesignTokens.swift) rather than bespoke off-black RGB values.
- **Evidence:** ContentDetailView.swift line 1296: `.fill(Color(red: 0.07, green: 0.06, blue: 0.05).opacity(0.42))` and line 1303: `color: Color(red: 0.05, green: 0.045, blue: 0.04).opacity(0.28)` inside floatingBackButton. These are raw Color(red:green:blue:) constructions for a content scrim/shadow, built outside DesignTokens.swift/ReaderPalette.swift. Not an onboarding/landing/watercolor file and not statusDestructive. Real low-severity tokenization gap (a near-neutral dark scrim that bypasses the token system); low severity correct.

### [LOW] token — Code-block background uses raw UIColor(red:...) light/dark literals instead of a surface token
- **File:** `Views/Components/SelectableMarkdownView.swift:322`
- **Issue:** appendCodeBlock() sets bgColor to UIColor(red: 0.14/0.15/0.17) (dark) and UIColor(red: 0.95/0.96/0.97) (light) at lines 322-323, duplicated again for inline code at lines 646-647. These hand-rolled neutral surface colors are built outside DesignTokens.swift/ReaderPalette.swift and approximate the surfaceContainer/surfaceSecondary family. There is no exact existing UIColor token for code-block backgrounds (only appSurfacePrimary is exposed as UIColor), so this is a real-but-low-severity tokenization gap rather than a hue violation.
- **Fix:** Add a UIColor surfaceContainer/code-background token in the UIColor extension of DesignTokens.swift (mirroring the existing Color.surfaceContainer) and reference it here so the light/dark code-block surface lives in the token system; reuse the same token for the inline-code case at lines 646-647.
- **Evidence:** SelectableMarkdownView.swift appendCodeBlock lines 322-323: `UIColor(red: 0.14, green: 0.15, blue: 0.17, alpha: 1)` (dark) / `UIColor(red: 0.95, green: 0.96, blue: 0.97, alpha: 1)` (light). Inline code lines 646-647: `UIColor(red: 0.2, green: 0.21, blue: 0.23, alpha: 1)` (dark) / `UIColor(red: 0.95, green: 0.96, blue: 0.97, alpha: 1)` (light) — note the dark inline value (0.2,0.21,0.23) differs slightly from the block value (0.14,0.15,0.17), so they are hand-rolled rather than a shared token. Raw neutral surface UIColors built outside DesignTokens.swift/ReaderPalette.swift, approximating surfaceContainer/surfaceSecondary. Real low-severity tokenization gap (no hue violation); low correct.

### [LOW] token — `detailTitleWeight` applies `.bold` on top of the `.appTitle3` serif token (already `.semibold`) for non-news detail titles
- **File:** `Views/ContentDetailView.swift:1266`
- **Issue:** `detailTitleFont` returns `.terracottaHeadlineCompact` (serif semibold) or `.appTitle3` (serif semibold); `detailTitleWeight` then overrides with `.semibold` for news and `.bold` for everything else (used at lines 1135 and 1200). The non-news branch re-weights `.appTitle3` to `.bold`, fighting the token's baked-in semibold. Unlike the other two findings this looks like a deliberate news-vs-other distinction, so it may be intentional, but it still bypasses the token's weight and is the same anti-pattern. Marked medium confidence because the dual-branch logic suggests intent.
- **Fix:** If the bold detail-title weight is intended, express it as a dedicated serif token (e.g. a bold detail-headline) so the weight lives in DesignTokens.swift; otherwise default both branches to the token weight and drop `detailTitleWeight`.
- **Evidence:** ContentDetailView.swift:1261-1267 confirmed. detailTitleFont returns .terracottaHeadlineCompact (news) or .appTitle3 (other); both tokens are .semibold per DesignTokens.swift:274 (terracottaHeadlineCompact = appSerif size:22 weight:.semibold) and :232 (appTitle3 = appSerif size:20 relativeTo:.title3 weight:.semibold). detailTitleWeight (line 1266) then returns .semibold for news and .bold for non-news, applied via .fontWeight() at lines 1135 and 1200. The non-news branch overrides the token's baked-in .semibold with .bold; the news branch redundantly re-applies .semibold. Real token-bypass anti-pattern. The dual-branch logic genuinely suggests a deliberate news-vs-other distinction, so low severity is honest.

### [LOW] token — `.italic()` synthesizes faux-italic on sans tokens instead of using the `appSansItalic`/`appSerifItalic` italic faces
- **File:** `Views/Components/ArticleCardView.swift:181`
- **Issue:** The design system ships real italic font faces via `Font.appSansItalic` (Roboto-Italic) and `Font.appSerifItalic` (Lora-Italic), but quote/hook text across the app applies `.italic()` on a regular sans token (e.g. `.font(.appSubheadline).italic()` here, and the same pattern at ArticleCardView.swift:228, StructuredSummaryView.swift:181, LongformArtifactView.swift:155, InterleavedSummaryV2View.swift:111, InterleavedSummaryView.swift:75, BulletedSummaryView.swift:72, EditorialNarrativeSummaryView.swift:114, SuggestionDetailSheet.swift:52/61). SwiftUI's `.italic()` on Roboto-Regular produces a synthetic oblique slant rather than the true italic face the token system provides, so italic content is typographically inconsistent with the design system's intent. Confidence medium because the visual difference is subtle and this is a widespread, possibly-tolerated convention.
- **Fix:** For italic body/quote text, switch to the dedicated italic tokens — use `Font.appSansItalic(size:)` (or a named italic token) instead of `.font(.appBody/.appCallout/.appSubheadline).italic()`. Consider adding named italic tokens (e.g. `appCalloutItalic`) to DesignTokens.swift to make the correct path easy at call sites.
- **Evidence:** Real italic font faces exist: DesignTokens.swift:201 appSansItalic uses AppFontFamily.sansItalic, :217 appSerifItalic uses AppFontFamily.serifItalic, and :275 terracottaHeadlineItalic = appSerifItalic. The cited sites apply .italic() on regular sans/serif tokens, synthesizing faux-italic: ArticleCardView.swift:181 (.appSubheadline), ArticleCardView.swift:228, InterleavedSummaryV2View.swift:111 (.appCallout), InterleavedSummaryView.swift:75 (.appCallout), LongformArtifactView.swift:155 (.appCallout), StructuredSummaryView.swift:181 (.appCallout), BulletedSummaryView.swift:72 (.appSubheadline), EditorialNarrativeSummaryView.swift:114 (.appCallout), SuggestionDetailSheet.swift:52/61 (.editorialBody). NOTE: the finding's paths say Views/ but the actual paths are Views/Components/ for all summary/card files; the issue itself is real and present at every cited line. Widespread possibly-tolerated convention, low severity appropriate.

### [LOW] token — Repeated VStack(spacing: 20) duplicates CardMetrics.cardSpacing across many call sites
- **File:** `Views/Library/FavoritesView.swift:78`
- **Issue:** `spacing: 20` literal appears in ~9 stacks (FavoritesView.swift:78, ChatSessionHistoryView.swift:100, Chat/ChatActivityViews.swift:64, Components/DiscoveryStateViews.swift:164, Components/CachedAsyncImage.swift:154, Components/TweetSuggestionsSheet.swift:18, Components/DetectedFeedCard.swift:80, Sources/SourceDetailSheet.swift:34, Components/ChatLoadingView.swift:16) while LongFormView.swift:76 correctly uses CardMetrics.cardSpacing (=20) for the same purpose. These literal 20s should route through the token so card/list inter-item spacing stays consistent.
- **Fix:** Replace VStack(spacing: 20) with VStack(spacing: CardMetrics.cardSpacing) where the value semantically means card/section spacing. Confirm each site is card-spacing rather than incidental layout before swapping.
- **Evidence:** CardMetrics.cardSpacing = 20 confirmed at DesignTokens.swift:355. LongFormView.swift:76 is the ONLY usage of CardMetrics.cardSpacing in the entire tree (grep -rln returned just that file). All 9 raw VStack(spacing: 20) literals confirmed at cited lines: FavoritesView.swift:78, ChatActivityViews.swift:64, DiscoveryStateViews.swift:164, CachedAsyncImage.swift:154, DetectedFeedCard.swift:80, ChatSessionHistoryView.swift:100, SourceDetailSheet.swift:34, TweetSuggestionsSheet.swift:18, ChatLoadingView.swift:16. Same card/list inter-item spacing purpose, bypassing the token. Low severity (consistency token-routing).

### [LOW] token — Row/section horizontal gutters use literal 12 instead of Spacing.rowHorizontal/screenHorizontal
- **File:** `Views/Chat/ChatComposerDock.swift:52`
- **Issue:** `.padding(.horizontal, 12)` is hardcoded across many content/row surfaces (ChatComposerDock.swift:52, Chat/MessageRow.swift:51, Chat/AssistantFeedOptionsSection.swift:215, Chat/ChatMessageList.swift:186, Chat/CouncilBranchTabs.swift:61/90, Chat/CouncilCandidatesBubble.swift:114, Chat/UserMessageBubble.swift:21, Shared/FormControls.swift:74, Components/QuickMicOverlay.swift:153/205/300/355, Components/TweetSuggestionsSheet.swift:97, Components/LongFormCardStackView.swift:259, Components/SuggestionDetailSheet.swift:107, KnowledgeView.swift:291, Settings/SettingsView.swift:309/732, ContentDetailView.swift:3207/3235, DebugMenuView.swift:43/61). 12 is exactly Spacing.screenHorizontal/rowHorizontal. These are the app's standard horizontal gutter and should reference the token so a future gutter change propagates.
- **Fix:** Replace .padding(.horizontal, 12) with .padding(.horizontal, Spacing.rowHorizontal) (or Spacing.screenHorizontal) on row/screen-content surfaces. Leave any genuinely fine-tuned 12s that are not the primary gutter.
- **Evidence:** Spacing.screenHorizontal = 12 (DesignTokens.swift:426) and Spacing.rowHorizontal = 12 (:433). Spot-checked .padding(.horizontal, 12) confirmed at ChatComposerDock.swift:52, MessageRow.swift:51, UserMessageBubble.swift:21, SettingsView.swift:309 (+732), ContentDetailView.swift:3207 (+3235). 17 files contain literal .padding(.horizontal, 12) while 18 files already use the Spacing.rowHorizontal/screenHorizontal token for the same gutter, confirming the inconsistency. Low severity.

### [LOW] token — Fast Read / dense gutters use literal 16 instead of Spacing.fastReadHorizontal
- **File:** `Views/ArticleReaderView.swift:141`
- **Issue:** `.padding(.horizontal, 16)` is hardcoded where Spacing.fastReadHorizontal (=16) is the intended token (ArticleReaderView.swift:141, KnowledgeDiscoveryView.swift:67, ChatSessionHistoryView.swift:60/69, KnowledgeView.swift:188, Settings/SettingsView.swift:357, Chat/ChatMessageList.swift:223, Chat/ChatErrorBanner.swift:58, Components/LongFormCard.swift:187, Components/FastReadBriefingComponents.swift:90, Components/CardStackView.swift:62, Components/ChatStatusBanner.swift:151). FastReadBriefingComponents and CardStackView are exactly the dense-headline surfaces fastReadHorizontal documents.
- **Fix:** Replace .padding(.horizontal, 16) with .padding(.horizontal, Spacing.fastReadHorizontal) on the Fast Read / dense-row surfaces. Verify each call site is a primary content gutter, not incidental padding.
- **Evidence:** Spacing.fastReadHorizontal = 16 confirmed at DesignTokens.swift:428, and is actively used in ShortFormView.swift (grep -rln). Cited literal .padding(.horizontal, 16) sites confirmed: ArticleReaderView.swift:141, FastReadBriefingComponents.swift:90, CardStackView.swift:62, LongFormCard.swift:187. The dense-headline/Fast-Read surfaces bypass the token that documents exactly this gutter. Low severity.

### [LOW] token — Hand-rolled list-row reset duplicates appListRow()
- **File:** `Views/SearchView.swift:84`
- **Issue:** SearchView.swift:84-85 applies `.listRowInsets(EdgeInsets())` + `.listRowBackground(Color.clear)` and elsewhere also hides separators, which is exactly what the appListRow() modifier (DesignTokens.swift:484) bundles (listRowInsets(EdgeInsets()) + listRowSeparator(.hidden) + listRowBackground(Color.clear)). The introSection (line 84) reproduces two of the three; appListRow() would cover it. ContentListView.swift:53-55 similarly hand-rolls the insets/background combo (though it intentionally keeps listRowSeparator(.visible), so only a partial match).
- **Fix:** Use .appListRow() at SearchView.swift:84 instead of the manual insets+background pair. For ContentListView.swift:53-55, consider an appListRow(separator: .visible) variant if the visible separator must be preserved.
- **Evidence:** appListRow() at DesignTokens.swift:484-488 bundles .listRowInsets(EdgeInsets()) + .listRowSeparator(.hidden) + .listRowBackground(Color.clear). SearchView.swift:84-85 (introSection EmptyStateView) hand-rolls .listRowInsets(EdgeInsets()) + .listRowBackground(Color.clear), reproducing two of the three. appListRow would cover it. Confirmed as a partial duplication of the bundled modifier. Low severity.

### [LOW] token — Raw Color literals for code-block / inline-code backgrounds outside DesignTokens
- **File:** `Views/Components/SelectableMarkdownView.swift:322`
- **Issue:** appendCodeBlock (lines 322-323) and inline code (lines 646-647) hardcode UIColor(red:green:blue:alpha:) literals for light/dark code backgrounds instead of using a surface token (e.g. surfaceContainer/surfaceContainerHigh). These are content backgrounds rendered in the reader, so per the project's token rule raw Color(red:green:blue:) used for content outside DesignTokens.swift/ReaderPalette.swift should be flagged. Low severity (neutral greys, not an accent hue) but it duplicates what existing surface tokens provide.
- **Fix:** Resolve a surface token (e.g. UIColor.appSurfaceContainer / surfaceContainerHighest) with traitCollection for the code background instead of hardcoded RGB literals, so it tracks the palette.
- **Evidence:** SelectableMarkdownView.swift appendCodeBlock lines 320-323: `bgColor = traitCollection... == .dark ? UIColor(red: 0.14, green: 0.15, blue: 0.17, alpha: 1) : UIColor(red: 0.95, green: 0.96, blue: 0.97, alpha: 1)`. Inline code lines 644-647: dark `UIColor(red: 0.2, green: 0.21, blue: 0.23, alpha: 1)`, light `UIColor(red: 0.95, green: 0.96, blue: 0.97, alpha: 1)`. These are raw UIColor(red:green:blue:) literals for reader content backgrounds, outside DesignTokens.swift/ReaderPalette.swift, duplicating what surfaceContainer/High tokens provide. Not an onboarding/landing/watercolor file. Neutral greys (not accent hue) and statusDestructive-exempt does not apply, so flag is valid at low severity per the token rule.

### [LOW] token — Uses Color.gray and raw corner radius literals instead of tokens
- **File:** `Views/Components/PlaceholderCard.swift:19`
- **Issue:** PlaceholderCard fills shimmer rectangles with `Color.gray.opacity(...)` (lines 19,24,35,43,52,58) and applies `.cornerRadius(12)` (74) and `.background(Color.surfaceSecondary)`. Per color doctrine, .gray where a neutral token exists (onSurfaceTertiary/surfaceContainer/skeleton) is an inconsistency, and 12 is a magic value not tied to a token.
- **Fix:** Swap Color.gray for a neutral surface/onSurface token (e.g. Color.onSurfaceTertiary.opacity) and consider a shared skeleton color; this is a low-priority consistency cleanup.
- **Evidence:** PlaceholderCard.swift: `Color.gray.opacity(...)` at lines 19, 24, 35, 44, 53, 59 (detail's cited 52/58 drift slightly to 53/59 but substance correct); `.cornerRadius(12)` at line 74; `.background(Color.surfaceSecondary)` at line 73. Component IS used (CardStackView.swift:44) so it ships. Per doctrine, .gray where a neutral token exists is a low-severity inconsistency and 12 duplicates a common metric value. Severity low correct.

### [LOW] token — Hardcoded cornerRadius(12) / surfaceSecondary card styling not using metric tokens
- **File:** `Views/Components/FilterBar.swift:81`
- **Issue:** FilterBar applies `.cornerRadius(12)` (81) on a card-like container. 12 is a magic literal where a metric token would normally apply. Note FilterBar itself also appears unused if it was tied to the dead ContentListView surface — worth confirming.
- **Fix:** Use an existing corner-radius token if one matches; verify whether FilterBar is still referenced before investing.
- **Evidence:** FilterBar.swift line 81: `.cornerRadius(12)` on a `.background(Color.surfaceSecondary)` (line 80) card container — magic literal where a metric token would apply. Additionally the finding's 'worth confirming' note is TRUE: grep -rn 'FilterBar' across newsly+ShareExtension returns ONLY the definition (FilterBar.swift:2 comment, :10 struct) — no call sites, so FilterBar is itself dead code. Token claim plus the dead-component observation both hold. Severity low accurate for the token issue.

### [MEDIUM] bug — Detached child Tasks in loadContent are not tied to task cancellation and can mutate after navigation
- **File:** `ViewModels/ContentDetailViewModel.swift:120`
- **Issue:** `loadContent()` spawns three unstructured `Task { await self.trackOpenedInteraction(...) }`, `Task { await self.loadContentBody(...) }`, and `Task { await self.markFetchedContentAsReadIfNeeded(...) }` (lines 120-132). These detached tasks are not children of the `.task(id: currentIndex)` scope, so when the user navigates (cancelling the outer task) these keep running. `loadContentBody` and `markFetchedContentAsReadIfNeeded` do guard with `self.contentId == fetched.id` before mutating state, but `markFetchedContentAsReadIfNeeded` still issues the mark-as-read network call and posts `.contentMarkedAsRead` + decrements unread counts for an item the user only briefly passed through during a fast cascade-skip, which can double-decrement counts or mark items read unintentionally.
- **Fix:** Make the child work structured (await within the task, or use `async let`/task group) so it is cancelled with the parent, or guard the network side-effects (not just the state mutation) on the request still being current before firing mark-as-read and count decrements.
- **Evidence:** ContentDetailViewModel.swift lines 120-132 spawn three bare `Task { await self.trackOpenedInteraction(...) }`, `Task { await self.loadContentBody(...) }`, `Task { await self.markFetchedContentAsReadIfNeeded(...) }`. These are unstructured Tasks, NOT children of the `.task(id: currentIndex)` scope at ContentDetailView.swift:429 that invokes loadContent(). They survive when the outer task is cancelled on navigation. loadContentBody (line 185) and markFetchedContentAsReadIfNeeded (line 231) guard self.contentId before mutating in-memory state, but the side-effecting network call still proceeds (see idx 30). Description is accurate.

### [MEDIUM] bug — Mark-as-read posts notification and decrements counts even when the user navigated away
- **File:** `ViewModels/ContentDetailViewModel.swift:236`
- **Issue:** In `markFetchedContentAsReadIfNeeded`, the stale guard `guard self.contentId == fetched.id` only protects the in-memory `content?.isRead = true` mutation. The subsequent `NotificationCenter.default.post(.contentMarkedAsRead ...)` and `unreadCountService.decrement*Count()` run regardless — but they are placed AFTER the guard returns, so they're actually skipped on stale. However the `contentService.markContentAsRead` network call (line 228) already fired before the guard, so during fast cascade navigation every transited item gets a server-side mark-as-read. Combined with the count-decrement logic keyed on `apiContentType`, rapidly skipping read items is fine (guarded by `!fetched.isRead`), but skipping unread items in a cascade marks them read server-side without the user actually reading them.
- **Fix:** Confirm this is desired: the auto-skip path is podcast-only and only triggers on already-read items, so unread items shouldn't be skipped — but the mark-as-read fires for any opened item before the staleness guard. Consider moving the network mark-as-read behind an explicit dwell/visibility check, or only mark read once the item has been the current item for a minimum duration.
- **Evidence:** markFetchedContentAsReadIfNeeded (lines 220-260): `contentService.markContentAsRead(id:...)` fires at line 228, which is BEFORE the stale guard `guard self.contentId == fetched.id` at line 231. The notification post (239-243) and unreadCountService.decrement*Count() (245-254) are correctly AFTER the guard so they are skipped on stale — exactly as the finding states. The only entry guard is `guard !fetched.isRead` (line 221), so transited UNREAD items get a server-side mark-as-read during fast cascade navigation even though the user never read them. Consequence is real.

### [MEDIUM] bug — Markdown colors can go stale on light/dark switch (no colorScheme dependency)
- **File:** `Views/Components/SelectableMarkdownView.swift:84`
- **Issue:** The markdown is rendered into `uiView.attributedText` with colors resolved at render time via `uiView.traitCollection` (resolvedTextColor/resolvedLinkColor and the per-run resolved colors inside MarkdownNSRenderer). The representable does not read @Environment(\.colorScheme), so SwiftUI is not guaranteed to re-invoke updateUIView on an appearance change. DigDeeperTextView.traitCollectionDidChange only calls applyAdaptiveTextColor(), which is a no-op for the markdown path because adaptiveTextColor is never set here (only the attributedText path is used). Result: an already-rendered article body can keep light-mode (or dark-mode) colors after the user toggles appearance while the view is on screen, until the RenderKey is otherwise invalidated. The RenderKey already includes color signatures, so the fix is just ensuring updateUIView runs on appearance change.
- **Fix:** Add `@Environment(\.colorScheme) private var colorScheme` to SelectableMarkdownView so SwiftUI re-invokes updateUIView on appearance change (it will then re-render because the color signatures in RenderKey differ), or override traitCollectionDidChange in DigDeeperTextView to also re-resolve/redraw the attributedText when no adaptiveTextColor is set.
- **Evidence:** SelectableMarkdownView.swift is a UIViewRepresentable that does NOT read @Environment(\.colorScheme). Colors are resolved at render time from uiView.traitCollection (lines 59-60: textColor.resolvedColor(with: uiView.traitCollection) and UIColor.appAccent.resolvedColor(...); lines 81-82 pass uiView.traitCollection into MarkdownNSRenderer). The result is set on uiView.attributedText (line 84) — the adaptiveTextColor path is never used here. Confirmed in DigDeeperTextView.swift: traitCollectionDidChange (lines 17-23) only calls applyAdaptiveTextColor(), which is guarded by `guard let adaptiveTextColor else { return }` (line 68). grep shows adaptiveTextColor is only ASSIGNED in SelectableText.swift:64, NOT in SelectableMarkdownView. So on a light/dark toggle while the markdown view is on screen, traitCollectionDidChange is a no-op for it, and with no colorScheme dependency SwiftUI is not guaranteed to re-run updateUIView, leaving stale attributedText colors. RenderKey already carries color signatures (lines 67-68), so the fix is just forcing updateUIView on appearance change. Real, medium.

### [MEDIUM] bug — Swipe haptic never fires: compares targetIndex to already-mutated currentIndex
- **File:** `Views/Components/LongFormCardStackView.swift:222`
- **Issue:** In handleDragEnd, currentIndex is assigned to targetIndex inside the withAnimation closure at lines 217-220 (a synchronous state write). Then line 222 checks `if targetIndex != currentIndex { triggerHaptic() }`. Because currentIndex now equals targetIndex, this condition is always false and the haptic never plays on a successful card change. Contrast PagedCardView.handleDragEnd, which correctly snapshots `oldIndex` before mutating and compares `targetIndex != oldIndex`.
- **Fix:** Capture `let oldIndex = currentIndex` at the top of handleDragEnd and compare `targetIndex != oldIndex` for the haptic (and for the load-more guard).
- **Evidence:** LongFormCardStackView.swift handleDragEnd: line 218 sets `currentIndex = targetIndex` synchronously inside the withAnimation closure (lines 217-220), THEN line 222 checks `if targetIndex != currentIndex { triggerHaptic() }`. Since currentIndex now equals targetIndex, the condition is always false and triggerHaptic() never runs on a successful card change. Contrast confirmed in PagedCardView.swift:119 `let oldIndex = currentIndex` snapshot taken BEFORE mutation (line 130), then line 134 `if targetIndex != oldIndex`. Real bug.

### [MEDIUM] bug — Non-optional array fields make whole summary silently nil if backend omits a key
- **File:** `Models/StructuredSummary.swift:13`
- **Issue:** StructuredSummary.bulletPoints/quotes/topics (lines 13-15) and InterleavedSummaryV2.keyPoints/topics/quotes (lines 101-103), BulletedSummary.points, EditorialNarrativeSummary.keyPoints/quotes are declared non-optional. The decode path uses `try? decoder.decode(...)` (ContentDetail.swift) which swallows errors, so if the API response omits any one of these keys (e.g. an article with no quotes serialized as a missing key rather than []), decoding throws and the ENTIRE summary becomes nil — the user sees no summary at all rather than a partial one. This is a silent failure mode that depends on backend serialization always emitting empty arrays.
- **Fix:** Make collection fields optional with a defaulting accessor, or implement a custom init(from:) using decodeIfPresent(...) ?? [] for each array field so a single missing key doesn't void the whole summary.
- **Evidence:** StructuredSummary.swift declares bulletPoints/quotes/topics non-optional (lines 13-15); InterleavedSummaryV2 keyPoints/topics/quotes non-optional (lines 101-103); BulletedSummary.points (line 132); EditorialNarrativeSummary keyPoints/quotes (lines 175-176). ContentDetail.swift decode path uses `try? decoder.decode(...)` for every variant (line 411 InterleavedSummary, 426 V2, 441 Bulleted, 456 Editorial, 470 Structured) — errors are swallowed, returning nil. So if the backend omits any one of these keys (vs serializing []), decoding throws and the ENTIRE summary becomes nil rather than partial. Note: the synthesized init(from:) for these structs uses plain `try container.decode([...])` (no decodeIfPresent / no default), so a missing key throws. structuredSummaryRaw is [String: AnyCodable]? (ContentDetail.swift:71). Real silent-failure mode dependent on backend always emitting empty arrays. Medium.

### [MEDIUM] bug — Markdown bold in shareText is shared as literal asterisks
- **File:** `Views/Components/ChatShareSheet.swift:19`
- **Issue:** ShareContent.shareText wraps the article title as "**\(title)**" but the value is passed to UIActivityViewController as a plain String (activityItems: [content.shareText]). UIActivityViewController does not interpret Markdown, so recipients (Messages, Mail, Notes, clipboard) see literal '**Title**' with asterisks rather than a bold/clean title. This is user-visible in every chat-message share.
- **Fix:** Drop the Markdown markers and share the plain title (e.g. "\(title)\n\n\(text)"), or build an NSAttributedString / provide a UIActivityItemSource that yields properly formatted content for share targets that support rich text.
- **Evidence:** ChatShareSheet.swift line 19: `text = "**\(title)**\n\n\(text)"` wraps the article title in literal Markdown asterisks. ShareSheet.makeUIViewController (line 34) sets `let activityItems: [Any] = [content.shareText]` and passes them to UIActivityViewController (line 35). UIActivityViewController does not interpret Markdown on a plain String, so recipients see literal '**Title**' with asterisks. Real, user-visible on every chat-message share.

### [MEDIUM] bug — Reconciler drops process-summary rows whose text lives in processLabel with empty content
- **File:** `ViewModels/ChatTimelineReconciler.swift:39`
- **Issue:** Incoming messages are filtered with `!$0.content.isEmpty || $0.hasCouncilCandidates`. Process-summary messages render via `processSummaryText` which is `processLabel ?? content` (see ChatMessage.processSummaryText and ProcessSummaryRow in Views/Chat/MessageBubble.swift, which displays `message.processSummaryText`). A process_summary message that carries its visible text in `processLabel` but has empty `content` (and no council candidates) is silently filtered out of the timeline, so the 'thinking' step disappears. The filter only protects against truly empty rows but does not account for processLabel-only summaries.
- **Fix:** Broaden the keep predicate to also retain process summaries that have non-empty processSummaryText, e.g. `!$0.content.isEmpty || $0.hasCouncilCandidates || ($0.isProcessSummary && !$0.processSummaryText.isEmpty)`.
- **Evidence:** ChatTimelineReconciler.swift line 39: `let incoming = detail.messages.filter { !$0.content.isEmpty || $0.hasCouncilCandidates }`. ChatMessage.swift:228-230 `var processSummaryText: String { processLabel ?? content }` and MessageBubble.swift:55 renders `Text(message.processSummaryText)`. A process_summary message whose visible text lives only in processLabel with empty content (and no council candidates, hasCouncilCandidates=false) fails both filter clauses and is dropped — its 'thinking' step disappears even though processSummaryText would render processLabel. The filter only guards truly empty rows and ignores processLabel-only summaries. Real, medium.

### [MEDIUM] bug — stopAudioCaptureAndDiscover has no re-entrancy guard; 30s auto-stop can collide with a manual stop
- **File:** `ViewModels/OnboardingViewModel.swift:271`
- **Issue:** handleAudioTimerTick (line 665) fires `Task { await stopAudioCaptureAndDiscover() }` once audioDurationSeconds >= 30, while the user can also tap stop in the same frame (the mic button is only disabled after audioState becomes .transcribing). Both invocations call dictationService.stop(). The second stop hits VoiceDictationService.finalizeRecordingAndTranscribe's `guard isRecording` / `guard !isFinalizing` and throws recordingFailed, which handleAudioError converts to audioState = .error and surfaces a spurious 'recording failed' message that clobbers the legitimately in-progress .transcribing state.
- **Fix:** Add an `isStoppingAudioCapture` guard at the top of stopAudioCaptureAndDiscover (set true on entry, reset in a defer) so the second concurrent call early-returns instead of issuing a second stop().
- **Evidence:** handleAudioTimerTick (OnboardingViewModel.swift:665-670) fires `Task { await stopAudioCaptureAndDiscover() }` when `audioDurationSeconds >= 30 && audioState == .recording`. stopAudioCaptureAndDiscover (line 271) has NO re-entrancy guard; it sets audioState=.transcribing then `await dictationService.stop()`. The mic button is disabled ONLY when audioState==.transcribing (OnboardingMicButton.swift:68), and handleTap calls onStop() only while .recording (lines 169-170). Because the timer's auto-stop is dispatched via Task (its `.transcribing` write is not synchronous with the tick), there is a window where the button still shows .recording and the user can tap, producing two concurrent stopAudioCaptureAndDiscover calls. The 2nd call's dictationService.stop()->finalizeRecordingAndTranscribe hits `guard isRecording`(VoiceDictationService.swift:305, now false after 1st call set isRecording=false at line 319) or `guard !isFinalizing`(line 308) and throws .recordingFailed; stop() calls onError? + rethrows (line 148-149); catch calls handleAudioError (OnboardingViewModel.swift:287) which sets audioState=.error (line 545), clobbering the legitimate .transcribing state. Mechanism and consequence are real; requires a tap landing in the narrow ~frame window at second 30. Confirmed (timing-dependent but real).

### [MEDIUM] bug — @AppStorage text-size indices inside ObservableObject do not emit objectWillChange
- **File:** `Services/AppSettings.swift:95`
- **Issue:** AppSettings is an ObservableObject and exposes appTextSizeIndex/contentTextSizeIndex (and other settings) via @AppStorage. @AppStorage is a DynamicProperty intended for Views; when it lives inside an ObservableObject, mutating it does NOT fire the object's objectWillChange publisher. The author clearly knows this — setReaderPalette() manually calls objectWillChange.send() before writing readerPaletteId (line 130) precisely to force a refresh. But the text-size sliders in SettingsView mutate settings.appTextSizeIndex / settings.contentTextSizeIndex directly (SettingsView.swift lines 408, 421) with no equivalent manual publish. Views that read these through @ObservedObject var settings = AppSettings.shared (e.g. ContentView.swift line 20 reading settings.contentTextSizeIndex / settings.appTextSizeIndex at lines 38 and 177, KnowledgeView, ChatSessionHistoryView) are therefore not reliably notified when the user drags the slider, so app/content text size can fail to update live until some other state change forces a redraw.
- **Fix:** Either add wrapper setters (like setReaderPalette) that call objectWillChange.send() for the text-size indices, or migrate AppSettings off @AppStorage-in-ObservableObject (e.g. use @Observable with explicit UserDefaults backing, or expose computed Int properties backed by UserDefaults with manual objectWillChange.send on write). Apply consistently to all @AppStorage-backed settings that drive UI.
- **Evidence:** AppSettings is `class AppSettings: ObservableObject` (AppSettings.swift:89) with `@AppStorage(...) var appTextSizeIndex`(line 95) and `contentTextSizeIndex`(line 96). The author demonstrably knows @AppStorage in an ObservableObject doesn't auto-fire objectWillChange: setReaderPalette() manually calls `objectWillChange.send()`(line 130) before writing readerPaletteId. The text-size sliders mutate the indices directly with NO equivalent publish: SettingsView.swift:408 `set: { settings.appTextSizeIndex = Int($0.rounded()) }` and line 421 for contentTextSizeIndex. SettingsView holds `@ObservedObject private var settings = AppSettings.shared`(line 11). Consumers read via the same @ObservedObject (ContentView.swift:20) at lines 38 (contentTextSizeIndex) and 177 (appTextSizeIndex); KnowledgeView.swift:64 and ChatSessionHistoryView.swift:16 also read appTextSizeIndex through settings. Mutating through the ObservableObject does not reliably fire objectWillChange, so live text-size updates can be missed until another state change forces a redraw. Confirmed.

### [MEDIUM] bug — FilterSheet filter changes reload the wrong list mode in Recently Read
- **File:** `Views/RecentlyReadView.swift:104`
- **Issue:** RecentlyReadView uses a ContentListViewModel and loads data via loadRecentlyRead(), which sets isRecentlyReadMode = true (ContentListViewModel.swift line 218). The FilterSheet is bound to viewModel.selectedContentType / selectedDate / selectedReadFilter, and each of those @Published properties has a didSet that fires Task { await loadContent() } (ContentListViewModel.swift lines 25-45). loadContent() sets isRecentlyReadMode = false and fetches the normal (non-recently-read) list. So as soon as the user changes any filter inside the sheet, the VM silently switches out of recently-read mode and loads regular content underneath the sheet. onDisappear then calls loadRecentlyRead() again, but loadRecentlyRead() ignores selectedContentType/selectedDate/selectedReadFilter entirely (line 222 fetches with no filter args), so the filter selections never actually apply to the recently-read list. Net effect: the filter UI appears functional but does nothing for Recently Read, and triggers a redundant wrong-mode fetch while open.
- **Fix:** Either make loadRecentlyRead honor the selected filters (pass selectedContentType/selectedDate through to fetchRecentlyReadList), or do not present the generic FilterSheet for this surface, or suppress the didSet-driven loadContent() while in recently-read mode (e.g. guard on isRecentlyReadMode) and reload via loadRecentlyRead() instead.
- **Evidence:** RecentlyReadView.task calls loadRecentlyRead() (RecentlyReadView.swift:92). FilterSheet binds to viewModel.selectedContentType/selectedDate/selectedReadFilter (lines 106-108); onDisappear re-calls loadRecentlyRead() (line 113). In ContentListViewModel each of those @Published props has a didSet firing `Task { await loadContent() }` (lines 27, 38, 43). loadContent() sets `isRecentlyReadMode = false`(line 61) and fetches the normal list. loadRecentlyRead() (line 213) sets isRecentlyReadMode=true(line 218) and calls `contentService.fetchRecentlyReadList(cursor: nil)`(line 222) with NO filter args — it ignores selectedContentType/selectedDate/selectedReadFilter entirely. So changing any filter in the sheet silently switches out of recently-read mode and loads regular content; on dismiss loadRecentlyRead() reloads but applies none of the filters. Net: filter UI does nothing for Recently Read and triggers a redundant wrong-mode fetch while open. Confirmed.

### [MEDIUM] bug — Polling upsert reorders the deck list on every refresh, causing decks to jump to the top
- **File:** `ViewModels/LearningDecksViewModel.swift:143`
- **Issue:** upsert() does decks.removeAll { $0.id == deck.id } then decks.insert(deck, at: 0). It is called from pollUntilLatestRunFinishes() every 3 seconds while a deck has an active run (and from refresh()/toggleShare()). For any actively-generating deck, each poll tick moves that deck back to position 0, so a list with multiple decks visibly re-orders/jumps while one is generating, and a refreshed deck unexpectedly leaps to the top. Identity is stable (LearningDeck.id) so SwiftUI animates the move rather than rebuilding, but the ordering is still wrong/unstable from the user's perspective.
- **Fix:** Make upsert replace in place when the deck already exists (find index and assign), and only insert at 0 for genuinely new decks created via createDeck(). Keep a stable sort (e.g., by createdAt/updatedAt) instead of last-touched-first.
- **Evidence:** upsert() (LearningDecksViewModel.swift:143-147) does `decks.removeAll { $0.id == deck.id }` then `decks.insert(deck, at: 0)`. pollUntilLatestRunFinishes calls `upsert(latest)` every poll tick (line 181), with pollingIntervalNanoseconds = 3_000_000_000 (3s, line 18) and pollingAttemptLimit 120. Initial load sets `decks = response.decks` in server order (line 32), but upsert (also called from refresh line 74 and toggleShare line 117) always moves the deck to index 0. For an actively-generating deck, each 3s poll re-inserts it at position 0, reordering a multi-deck list and making a refreshed/toggled deck jump to the top. Confirmed.

### [MEDIUM] bug — Whole-screen single-tap dismiss conflicts with double-tap-to-zoom
- **File:** `Views/Components/FullImageView.swift:77`
- **Issue:** A `.onTapGesture { dismiss() }` is attached to the outer ZStack (line 77), while the image has `.onTapGesture(count: 2)` for zoom (line 45). Because the single-tap recognizer covers the image too, a tap that begins a double-tap can be consumed by the parent single-tap and dismiss the viewer, making zoom-in hard/unreliable to trigger and causing accidental dismissals when the user intends to zoom. Gesture arbitration between an ancestor count:1 tap and a descendant count:2 tap is fragile.
- **Fix:** Scope dismissal so it doesn't overlap the zoom target — e.g. dismiss only via a background tap area behind the image, or use `.highPriorityGesture`/`.simultaneousGesture` with explicit ordering so the double-tap wins over the dismiss tap, or require the single-tap dismiss only when scale == 1.0.
- **Evidence:** FullImageView.swift: outer ZStack has `.onTapGesture { dismiss() }` (lines 77-79), and the image has `.onTapGesture(count: 2)` for zoom (lines 45-55). The ancestor count:1 tap covers the image region. SwiftUI gesture arbitration between an ancestor single-tap and a descendant double-tap is fragile: a tap intended to begin a double-tap can be consumed by the parent single-tap, dismissing the viewer and making zoom-by-double-tap unreliable / causing accidental dismissals. Code matches the finding exactly (cited lines 77 and 45 are correct). Confirmed.

### [MEDIUM] bug — Add sheet dismisses and clears form even when addConfig fails
- **File:** `Views/Sources/FeedSourcesView.swift:181`
- **Issue:** The 'Add' button does `await viewModel.addConfig(...)` then UNCONDITIONALLY calls `resetAddForm()` and `showAddSheet = false`. `ScraperSettingsViewModel.addConfig` swallows errors internally and only sets `errorMessage` (it never throws). So on a network/validation failure the sheet closes and the user's typed URL/name are wiped, leaving only a tiny error banner on the underlying list. This is inconsistent with `SourceDetailSheet.saveChanges()`, which correctly guards `if viewModel.errorMessage == nil { dismiss() }`.
- **Fix:** After `await viewModel.addConfig(...)`, only reset and dismiss when `viewModel.errorMessage == nil`; otherwise keep the sheet open so the user can correct and retry.
- **Evidence:** FeedSourcesView.swift:181-191 Add button: `await viewModel.addConfig(...)` is followed UNCONDITIONALLY by `resetAddForm()` (line 188) and `showAddSheet = false` (line 189). ScraperSettingsViewModel.addConfig (lines 79-93) is `async` (not throwing): it wraps the call in do/catch and on failure only sets `errorMessage = error.localizedDescription` (line 91) and returns normally. So a failed add still dismisses the sheet and wipes newFeedURL/newFeedName. This contrasts with SourceDetailSheet.saveChanges() (line 311) which guards `if viewModel.errorMessage == nil { dismiss() }`. Confirmed.

### [MEDIUM] bug — Add podcast sheet dismisses and clears form even when addConfig fails
- **File:** `Views/Sources/PodcastSourcesView.swift:192`
- **Issue:** Same defect as FeedSourcesView: after `await viewModel.addConfig(...)` the code always calls `resetAddForm()` and `showAddSheet = false`. Because `addConfig` never throws (it sets `errorMessage` and returns), a failed add still closes the sheet and discards the typed feed URL/name/limit, surfacing only an error banner behind the dismissed sheet.
- **Fix:** Gate `resetAddForm()` / `showAddSheet = false` on `viewModel.errorMessage == nil` after the await, mirroring SourceDetailSheet.saveChanges().
- **Evidence:** PodcastSourcesView.swift:193-200 Add button: after `await viewModel.addConfig(scraperType: "podcast_rss", ..., limit: limitValue)` it unconditionally calls `resetAddForm()` (line 199) and `showAddSheet = false` (line 200). Same `addConfig` (ScraperSettingsViewModel lines 79-93) never throws and only sets errorMessage on failure. resetAddForm (lines 210-214) clears newFeedURL/newFeedName/newLimit. So a failed add closes the sheet and discards typed URL/name/limit. Confirmed; identical defect to idx 51.

### [MEDIUM] bug — Long-content tab does not refresh in background when already loaded, unlike short-news
- **File:** `ViewModels/TabCoordinatorViewModel.swift:85`
- **Issue:** ensureTabLoaded handles .shortNews by calling refreshInBackground() when items already exist, but the .longContent branch only logs 'already available' and does nothing when currentItems() is non-empty. The result is asymmetric behavior: returning to the Fast tab silently refreshes stale content, but returning to the Long tab never does (no background refresh, no refreshTrigger). If this asymmetry is intentional it should be documented; otherwise long-form content can go stale indefinitely across tab switches while fast news stays fresh.
- **Fix:** Mirror the shortNews branch: call longContentVM.refreshInBackground() in the non-empty case, or add a comment explaining why long-form deliberately skips background refresh.
- **Evidence:** TabCoordinatorViewModel.swift ensureTabLoaded (lines 75-98): the .shortNews branch (77-84) calls `shortNewsVM.refreshInBackground()` when items already exist (else clause line 81-83), but the .longContent branch (85-91) only logs 'tab content already available' and does nothing in its else (line 89-90) when currentItems() is non-empty. Both VMs inherit `refreshInBackground()` from BaseContentListViewModel (BaseContentListViewModel.swift:82), so the long branch COULD refresh but doesn't. Asymmetry is real: Fast tab silently refreshes stale content on return, Long tab never does. Medium appropriate (stale long-form content across tab switches).

### [MEDIUM] deadcode — VoiceFFTAnalyzer enum and its only method are unreferenced (dead file)
- **File:** `Services/VoiceFFTAnalyzer.swift:9`
- **Issue:** `enum VoiceFFTAnalyzer` and its sole public member `normalizedEnergy(fromPCM16:)` (line 12) appear ONLY in this file. grep across newsly+ShareExtension for both `VoiceFFTAnalyzer` and `normalizedEnergy` returns only their own definitions (0 call sites). The file is auto-compiled via the project's PBXFileSystemSynchronizedRootGroup, so it ships dead.
- **Fix:** Delete Services/VoiceFFTAnalyzer.swift.
- **Evidence:** grep for both `VoiceFFTAnalyzer` and `normalizedEnergy` across newsly+ShareExtension returns only matches inside VoiceFFTAnalyzer.swift itself (the comment line 2, the enum def line 9, the method def line 12). Zero external call sites. Word-boundary file list: only VoiceFFTAnalyzer.swift. Project uses PBXFileSystemSynchronizedRootGroup (6 occurrences in project.pbxproj) so the file auto-compiles and ships dead. Confirmed dead.

### [MEDIUM] deadcode — ChatGPTDeepLink class is never used (dead file)
- **File:** `Services/ChatGPTDeepLinkService.swift:6`
- **Issue:** `final class ChatGPTDeepLink` plus its static methods `openPreferApp(fallbackWebURL:completion:)` and `openUniversalOrWeb(_:)` have zero references outside this 35-line file (grep for `ChatGPTDeepLink`, `openPreferApp`, `openUniversalOrWeb` each return only the definitions).
- **Fix:** Delete Services/ChatGPTDeepLinkService.swift.
- **Evidence:** grep for `ChatGPTDeepLink`, `openPreferApp`, `openUniversalOrWeb` returns only definitions inside ChatGPTDeepLinkService.swift (class line 6, methods lines 15 and 32). Word-boundary file list: only ChatGPTDeepLinkService.swift. No external references. Auto-included via synchronized root group. Confirmed dead.

### [MEDIUM] deadcode — PagedCardView is never instantiated (dead file)
- **File:** `Views/Components/PagedCardView.swift:11`
- **Issue:** `struct PagedCardView` is referenced nowhere as a type. The only other hit for the name is a comment in Models/NewsGroup.swift:17 ('Card padding applied in PagedCardView'), not a usage. Its helper funcs (dragGesture/handleDragEnd) only collide by name with same-named methods in the already-dead LongFormCardStackView.
- **Fix:** Delete Views/Components/PagedCardView.swift (and drop the stale NewsGroup.swift:17 comment).
- **Evidence:** `PagedCardView` (word boundary) appears in exactly two files: PagedCardView.swift (its own def, line 11) and Models/NewsGroup.swift:17 which is a comment (`// Card padding applied in PagedCardView`), not a usage. `grep PagedCardView(` returns no instantiations. The view is never instantiated as a type. Confirmed dead.

### [MEDIUM] deadcode — AncientScrollRevealView + RevealPhysicsScene onboarding effect is dead
- **File:** `Views/Onboarding/AncientScrollRevealView.swift:12`
- **Issue:** `struct AncientScrollRevealView` has zero external references (grep returns only its own def; other onboarding views HowItWorksModal/OnboardingFlowView/OnboardingMicButton are all referenced, this one is not). `RevealPhysicsScene` (RevealPhysicsScene.swift:140) and its helpers GlyphPhraseCycler/SwipeImpulseModel/GlyphDepth/LCG are used ONLY by AncientScrollRevealView, so the whole SpriteKit reveal scene is transitively dead.
- **Fix:** Delete Views/Onboarding/AncientScrollRevealView.swift and Views/Onboarding/RevealPhysicsScene.swift.
- **Evidence:** `AncientScrollRevealView` (word boundary) appears only in its own file; `grep AncientScrollRevealView(` returns no external instantiation. Sanity-checked siblings: HowItWorksModal and OnboardingFlowView ARE referenced (AuthenticatedRootView.swift:32,49), so the grep method is sound. RevealPhysicsScene/GlyphPhraseCycler/SwipeImpulseModel/GlyphDepth/LCG appear only in RevealPhysicsScene.swift and AncientScrollRevealView.swift (the latter instantiates RevealPhysicsScene at line 19) — so the whole SpriteKit reveal scene is transitively dead. Confirmed.

### [MEDIUM] deadcode — FilterBar view is unreferenced (dead file)
- **File:** `Views/Components/FilterBar.swift:10`
- **Issue:** `struct FilterBar` appears only in its own file (grep `FilterBar` whole-word -> def only). Filtering UI is provided elsewhere (FilterSheet.swift exists and is used); this standalone bar is not wired up.
- **Fix:** Delete Views/Components/FilterBar.swift.
- **Evidence:** `FilterBar` (word boundary) appears only in FilterBar.swift; `grep FilterBar(` returns no instantiation. Filtering UI is provided by FilterSheet.swift (confirmed: `struct FilterSheet` exists at newsly/Views/Components/FilterSheet.swift). Standalone FilterBar is not wired up. Confirmed dead.

### [MEDIUM] deadcode — ContentTypeBadge view is unreferenced (dead file)
- **File:** `Views/Components/ContentTypeBadge.swift:10`
- **Issue:** `struct ContentTypeBadge` is referenced nowhere outside its own file (grep -> def only). Content-type labeling in feeds/cards is handled by other components; this badge is unused.
- **Fix:** Delete Views/Components/ContentTypeBadge.swift.
- **Evidence:** `ContentTypeBadge` (PascalCase struct, word boundary) appears only in ContentTypeBadge.swift; `grep ContentTypeBadge(` returns no instantiation. Note it is distinct from the lowercase `contentTypeBadge` computed property inside ArticleCardView.swift (different identifier). No external usage of the struct. Confirmed dead.

### [MEDIUM] deadcode — DownloadMoreMenu view is unreferenced (dead file)
- **File:** `Views/Components/DownloadMoreMenu.swift:8`
- **Issue:** `struct DownloadMoreMenu` is referenced nowhere outside its own file (grep -> def only).
- **Fix:** Delete Views/Components/DownloadMoreMenu.swift.
- **Evidence:** grep -rn 'DownloadMoreMenu' across newsly + ShareExtension (build dirs excluded) yields only 2 hits, both in DownloadMoreMenu.swift itself: line 2 (file-header comment) and line 8 (`struct DownloadMoreMenu: View {`). No call sites, no string-literal references. Confirmed dead file.

### [MEDIUM] deadcode — SelectableText UIViewRepresentable is unreferenced (dead file)
- **File:** `Views/Components/SelectableText.swift:9`
- **Issue:** `struct SelectableText` (a UIViewRepresentable) is referenced nowhere outside its own file. The app uses SelectableMarkdownView / SelectableText-like behavior elsewhere, but this specific type has zero usages (the apparent `Coordinator`/`makeUIView` matches are same-named methods on other representables, not this type).
- **Fix:** Delete Views/Components/SelectableText.swift.
- **Evidence:** grep -rnw 'SelectableText' yields only the comment at line 2 and the definition at line 9 (`struct SelectableText: UIViewRepresentable {`). Zero usages of this exact type anywhere in the source tree. No string-literal ref. Confirmed dead file.

### [MEDIUM] deadcode — KnowledgeDiscoveryView is never navigated to (dead file)
- **File:** `Views/KnowledgeDiscoveryView.swift:8`
- **Issue:** `struct KnowledgeDiscoveryView` is referenced nowhere outside its own file (grep whole-word -> def only). The Knowledge surface is served by KnowledgeView; this discovery variant is unwired.
- **Fix:** Delete Views/KnowledgeDiscoveryView.swift.
- **Evidence:** grep -rnw 'KnowledgeDiscoveryView' yields only line 2 (comment) and line 8 (`struct KnowledgeDiscoveryView: View {`). The real Knowledge surface is KnowledgeView (newsly/Views/KnowledgeView.swift exists). KnowledgeDiscoveryView is never navigated to or instantiated. Confirmed dead file.

### [MEDIUM] deadcode — AuthenticationView superseded by LandingView (dead file)
- **File:** `Views/AuthenticationView.swift:12`
- **Issue:** The app root (newslyApp.swift:33) shows `LandingView()` for the unauthenticated state and `AuthenticatedRootView` for authenticated; `AuthenticationView` (struct) is instantiated only in its own #Preview (AuthenticationView.swift:103). Its `handleLogoTap()` is a private method duplicated in LandingView, not a cross-file link. The view is dead.
- **Fix:** Delete Views/AuthenticationView.swift (keep AuthenticationViewModel, which is heavily used).
- **Evidence:** grep -rnw 'AuthenticationView' yields only: line 2 (comment), line 12 (`struct AuthenticationView: View {`), and line 103 (`AuthenticationView()`) which is inside the `#Preview` block (line 102). newslyApp.swift:30 uses AuthenticatedRootView and :33 uses LandingView for the root states; AuthenticationView is never instantiated outside its own preview. Confirmed dead file.

### [MEDIUM] deadcode — ContentListView struct is never used (dead file)
- **File:** `Views/ContentListView.swift:10`
- **Issue:** `struct ContentListView` is instantiated only in its own ContentListView_Previews (line 124). Real screens use `ContentListViewModel` (a different type) directly (RecentlyReadView, FavoritesView). The view struct itself is unreachable from navigation.
- **Fix:** Delete Views/ContentListView.swift (retain ViewModels/ContentListViewModel.swift, which is used).
- **Evidence:** grep -rnw 'ContentListView' yields only: line 2 (comment), line 10 (`struct ContentListView: View {`), and line 124 (`ContentListView()`) which sits inside ContentListView_Previews (PreviewProvider at line 122). The only instantiation is preview-only; no navigation reaches it. Note ContentListViewModel is a distinct type. Confirmed dead file.

### [MEDIUM] deadcode — ArticleDetailViewModel is unreferenced (dead file)
- **File:** `ViewModels/ArticleDetailViewModel.swift:12`
- **Issue:** `class ArticleDetailViewModel` is referenced nowhere outside its own file (grep whole-word -> def only). Its `markAsRead` only collides by name with markAsRead methods on other view models/stores. Article detail is driven by ContentDetailViewModel.
- **Fix:** Delete ViewModels/ArticleDetailViewModel.swift.
- **Evidence:** grep -rnw 'ArticleDetailViewModel' yields only line 2 (comment) and line 12 (`class ArticleDetailViewModel: ObservableObject {`). No instantiation anywhere. Article detail is driven by ContentDetailViewModel (newsly/ViewModels/ContentDetailViewModel.swift exists). Confirmed dead file.

### [MEDIUM] deadcode — PodcastDetailViewModel is unreferenced (dead file)
- **File:** `ViewModels/PodcastDetailViewModel.swift:12`
- **Issue:** `class PodcastDetailViewModel` is referenced nowhere outside its own file (grep whole-word -> def only).
- **Fix:** Delete ViewModels/PodcastDetailViewModel.swift.
- **Evidence:** grep -rnw 'PodcastDetailViewModel' yields only line 2 (comment) and line 12 (`class PodcastDetailViewModel: ObservableObject {`). No instantiation or reference anywhere in the source tree. No string-literal ref. Confirmed dead file.

### [MEDIUM] deadcode — NewsGroupViewModel is unreferenced (dead file)
- **File:** `ViewModels/NewsGroupViewModel.swift:13`
- **Issue:** `class NewsGroupViewModel: CursorPaginatedViewModel` is referenced nowhere outside its own file (grep whole-word -> def only). Its `refresh()` only collides by name with refresh methods across many other view models.
- **Fix:** Delete ViewModels/NewsGroupViewModel.swift.
- **Evidence:** grep -rn 'NewsGroupViewModel' across newsly/ + ShareExtension/ (excluding build dirs) returns ONLY its own file: NewsGroupViewModel.swift:2 (comment) and :13 (class def). grep for 'NewsGroupViewModel(' returns zero instantiation sites. No xib/storyboard/plist reference. It is a fully-implemented 191-line @MainActor view model (loadNewsGroups, loadMoreGroups, markGroupAsRead, etc.) that is completely orphaned. Confirmed dead.

### [MEDIUM] deadcode — SelectableText UIViewRepresentable is entirely unused
- **File:** `Views/Components/SelectableText.swift:9`
- **Issue:** The struct SelectableText (the plain-text selectable wrapper, distinct from SelectableMarkdownView) is never referenced anywhere in newsly/ or ShareExtension/. A whole-codebase grep for `SelectableText` returns only its own definition file. It carries a non-trivial Coordinator, tap gesture wiring, and line-limit sizing logic that is all dead. This is maintenance overhead that drifts out of sync with the live DigDeeperTextView wrapper.
- **Fix:** Delete Views/Components/SelectableText.swift, or wire it in if it was intended to replace a Text usage. If kept intentionally for a near-future feature, add a comment noting that.
- **Evidence:** SelectableText.swift:9 `struct SelectableText: UIViewRepresentable` with onDigDeeper/onTap closures, lineLimit/lineBreakMode and a Coordinator. grep -rn 'SelectableText' across both source roots returns only its own file (lines 2 and 9); grep for 'SelectableText(' returns zero call sites. Distinct from SelectableMarkdownView. Confirmed entirely unused dead code.

### [MEDIUM] deadcode — Theme.chat (MarkdownUI theme) is defined but never applied
- **File:** `Views/Components/ChatMarkdownTheme.swift:14`
- **Issue:** `static let chat = Theme()` is never referenced via `.markdownTheme(.chat)` or any other call site anywhere in newsly/ or ShareExtension/ (grep for Theme.chat / markdownTheme returns nothing outside this file). The entire ~140-line theme, plus the `import MarkdownUI` dependency it pulls in, is dead. Chat bubbles render via SelectableMarkdownView's hand-rolled MarkdownNSRenderer instead, so this MarkdownUI theme is orphaned.
- **Fix:** Remove ChatMarkdownTheme.swift (and the MarkdownUI dependency if nothing else uses it), or apply `.markdownTheme(.chat)` at the intended chat-bubble call site if MarkdownUI rendering was the goal.
- **Evidence:** ChatMarkdownTheme.swift:14 `static let chat = Theme()` (extension Theme, ~140 lines, requires `import MarkdownUI` line 8). grep for 'Theme.chat', '.markdownTheme(.chat)', and '.markdownTheme' across newsly/ + ShareExtension/ returns NO matches outside this file. The `.chat` grep hits elsewhere are an unrelated DetailSheetDestination/linkHandlingMode enum case `.chat` (ContentDetailView, ShareViewController) — none reference this Theme. The theme is defined but never applied; orphaned. Confirmed dead.

### [MEDIUM] deadcode — AncientScrollRevealView is never instantiated (entire file is dead code)
- **File:** `Views/Onboarding/AncientScrollRevealView.swift:12`
- **Issue:** AncientScrollRevealView is defined but has zero call sites anywhere in newsly/ or ShareExtension/ (a grep for the type only matches its own definition and self-references). It is a fairly heavy SpriteKit-backed view. Shipping it adds binary size, a SpriteKit dependency, and ongoing maintenance burden for code that no user ever sees.
- **Fix:** Delete AncientScrollRevealView.swift, or if it is intended for an upcoming flow, gate it behind a feature flag and add the call site so it is exercised. Confirm with the author whether this was abandoned WIP.
- **Evidence:** grep for 'AncientScrollRevealView' across newsly/ and ShareExtension/ (excluding build/dep dirs) returns ONLY its own file: line 2 (doc comment) and line 12 (struct definition). Zero instantiation/call sites anywhere. It is a SpriteKit-backed view (imports SpriteKit, uses SpriteView + RevealPhysicsScene at line 19/33). RevealPhysicsScene itself is only referenced from this dead view (RevealPhysicsScene.swift:140 definition + AncientScrollRevealView.swift:19), so the SpriteKit physics scene is effectively reachable only through dead code. Confirmed dead. Medium.

### [MEDIUM] deadcode — RevealPhysicsScene (and SwipeImpulseModel) only consumed by the unused AncientScrollRevealView
- **File:** `Views/Onboarding/RevealPhysicsScene.swift:140`
- **Issue:** RevealPhysicsScene is referenced only by AncientScrollRevealView, which is itself never instantiated. SwipeImpulseModel is used only by RevealPhysicsScene. So this whole physics subsystem (LCG generator, word bank, glyph spawning) is unreachable dead code that still compiles into the app.
- **Fix:** Delete RevealPhysicsScene.swift alongside AncientScrollRevealView.swift, or restore the call site if the reveal animation is meant to ship.
- **Evidence:** Grep across newsly/ + ShareExtension/ (excluding build dirs): `AncientScrollRevealView` appears only at its own file header comment and `struct AncientScrollRevealView: View` (Views/Onboarding/AncientScrollRevealView.swift:2,12) — zero instantiations anywhere. `RevealPhysicsScene` is referenced only inside RevealPhysicsScene.swift (def at line 140) and AncientScrollRevealView.swift:19 (`@State private var physicsScene = RevealPhysicsScene(...)`). `SwipeImpulseModel` appears only at its definition (RevealPhysicsScene.swift:38) and one use inside RevealPhysicsScene itself (line 237 `SwipeImpulseModel.impulseVector(...)`). Since AncientScrollRevealView is never instantiated, the whole subsystem (RevealPhysicsScene + SwipeImpulseModel + GlyphPhraseCycler) is unreachable dead code. Confirmed.

### [MEDIUM] deadcode — Entire Discovery-suggestions surface appears unreferenced (dead feature)
- **File:** `Views/KnowledgeDiscoveryView.swift:8`
- **Issue:** grep across newsly/ and ShareExtension/ shows KnowledgeDiscoveryView is never instantiated anywhere (only its own file matches). It is the sole consumer of DiscoveryViewModel, which is the sole consumer of DiscoveryService.shared and of DiscoveryRunSection / DiscoverySuggestionCard / SuggestionDetailSheet / DiscoveryStateViews / (the dead path into) DiscoveryPersonalizeSheet. So this whole cluster of files is currently dead code shipping in the binary. Worth confirming before deletion in case it is wired up via a feature flag or planned screen.
- **Fix:** Confirm with the team whether the Discover tab is shipped/feature-flagged. If truly unused, remove KnowledgeDiscoveryView + DiscoveryViewModel + DiscoveryService + the Discovery* component files; otherwise wire it into navigation.
- **Evidence:** `KnowledgeDiscoveryView(` instantiation grep across newsly/ + ShareExtension/ returns ZERO matches; the symbol only appears at its own file (KnowledgeDiscoveryView.swift:2,8). `DiscoveryViewModel(` instantiation: ZERO matches; DiscoveryViewModel is referenced only in its own file and the dead KnowledgeDiscoveryView (grep excluding those two files returns nothing). DiscoveryService referenced only by DiscoveryViewModel + its own file. Downstream components are reachable ONLY through the dead view: DiscoveryRunSection used at KnowledgeDiscoveryView.swift:192; SuggestionDetailSheet at :43; DiscoveryPersonalizeSheet at :37; DiscoveryStateViews types (DiscoveryLoadingStateView/ErrorStateView/EmptyStateView) used only at KnowledgeDiscoveryView.swift:80,82,86; DiscoverySuggestionCard used only inside DiscoveryRunSection. So the entire Discovery cluster is dead code (note: OnboardingSuggestionCard/LaneStatusRow are shared with onboarding and are NOT dead, but those weren't claimed dead). Confirmed.

### [MEDIUM] deadcode — ContentListView is unreferenced (dead view)
- **File:** `Views/ContentListView.swift:10`
- **Issue:** Grep across newsly/ and ShareExtension/ finds no construction of `ContentListView()` anywhere except its own `ContentListView_Previews`. The live content surfaces are ShortFormView/LongFormView/RecentlyReadView/FavoritesView. The whole file (including its embedded LoadingView/ErrorView usage and floating filter button) appears to be legacy and can be removed, which also eliminates the only call site of the unused VM method markAsRead's swipe path here.
- **Fix:** Confirm no storyboard/dynamic reference, then delete Views/ContentListView.swift (and its preview).
- **Evidence:** Grep for `ContentListView` (whole-word, struct, not the ViewModel) shows the only references are its own definition at Views/ContentListView.swift:10 and its `ContentListView_Previews` at lines 122/124. No `ContentListView()` construction exists in any live view, tab coordinator, or AppChrome. The live surfaces (LongFormView, RecentlyReadView, FavoritesView) construct `ContentListViewModel()` directly but never the view. The file embeds a full NavigationStack + filter button + markAsRead swipe path that is unreachable. Confirmed dead view.

### [MEDIUM] deadcode — selectedContentTypes is dead state; its branch is unreachable
- **File:** `ViewModels/ContentListViewModel.swift:31`
- **Issue:** `selectedContentTypes` is never assigned from any view (no external writes found). It is initialized to [] and only read internally, so the `if !selectedContentTypes.isEmpty` branches in loadContent() (68-74) and loadMoreContent() (112-118) can never execute. The property also carries a didSet that would trigger loadContent(), adding to the race surface for no benefit.
- **Fix:** Remove `selectedContentTypes`, its didSet, and the dead `!isEmpty` branches, collapsing to the selectedContentType path; or wire up a real setter if multi-type filtering is intended.
- **Evidence:** Grep for `selectedContentTypes` returns 7 hits, ALL inside ViewModels/ContentListViewModel.swift (def at line 31; internal reads at 67/68/70 and 111/112/114). No external assignment exists anywhere in newsly/ or ShareExtension/. It is initialized to `[]` and never mutated, so `if !selectedContentTypes.isEmpty` at lines 68 and 112 is always false and the contentTypes branch is unreachable. The property also carries a `didSet { Task { await loadContent() } }` (lines 32-34) that can never fire. Confirmed dead state with unreachable branch.

### [MEDIUM] deadcode — QuickMicOverlay view (and WaveformGlyph) are never instantiated anywhere
- **File:** `Views/Components/QuickMicOverlay.swift:3`
- **Issue:** `QuickMicOverlay` is defined but there is no call site in the app or ShareExtension (grep for `QuickMicOverlay(` returns nothing). The private helpers `QuickMicMessageBubble`, `QuickMicStatusBubble`, and the public `WaveformGlyph` (no `WaveformGlyph` references exist) are also dead. Only `TapToTalkMicButton` from this file and the separate `QuickMicViewModel` are actually used elsewhere. This is a large block of unreferenced UI carrying its own forced color-scheme overrides and raw `Color.black`/`.secondary`/`Color.gray` usage.
- **Fix:** Remove QuickMicOverlay, QuickMicMessageBubble, QuickMicStatusBubble, and WaveformGlyph (move TapToTalkMicButton to its own file), or wire the overlay into a screen if it is intended to ship.
- **Evidence:** Grep `QuickMicOverlay(` returns nothing; the only `QuickMicOverlay` reference is its own struct def at Views/Components/QuickMicOverlay.swift:3. `WaveformGlyph` has only its def at line 426 (no `WaveformGlyph(` call sites). The private helpers QuickMicMessageBubble/QuickMicStatusBubble are only used inside the dead overlay body. By contrast `TapToTalkMicButton` (also defined in this file at line 372) IS live: used in KnowledgeView.swift:511, Chat/ChatComposerDock.swift:163, Components/LearningDeckCreateSheet.swift:82. So the dead block is the overlay struct + its private bubbles + WaveformGlyph, while TapToTalkMicButton must be retained. Finding's scoping is accurate. Confirmed.

### [MEDIUM] deadcode — QuickMicHost / quickMicContext preference machinery is entirely unused (and QuickMicHost ignores its own inputs)
- **File:** `Views/Components/QuickMicContext.swift:46`
- **Issue:** `QuickMicHost` stores `tab`, `viewModel`, `fallbackContext`, `fallbackAllowsQuickMic`, `onOpenChatSession` but its `body` just returns `content()` — every stored property is dead. None of `QuickMicHost(`, `.quickMicContext(`, `QuickMicContextPreferenceKey`, `QuickMicContextModifier`, or `QuickMicContextPayload` are referenced outside this file (verified by grep). The whole context/preference plumbing is dead code.
- **Fix:** Delete QuickMicHost, QuickMicContextModifier, the quickMicContext(...) View extension, QuickMicContextPreferenceKey, and QuickMicContextPayload, or finish wiring them into the tab shell.
- **Evidence:** All references to QuickMicHost, quickMicContext, QuickMicContextPreferenceKey, QuickMicContextModifier, and QuickMicContextPayload are confined to Views/Components/QuickMicContext.swift (verified by grep across newsly/ + ShareExtension/). No `QuickMicHost(` or `.quickMicContext(` call site exists in any live view. QuickMicHost.body (lines 54-56) just returns `content()`, ignoring its stored tab/viewModel/fallbackContext/fallbackAllowsQuickMic/onOpenChatSession (lines 47-51). The whole preference/context plumbing is unused dead code. Confirmed.

### [MEDIUM] deadcode — PodcastDetailViewModel is never instantiated (whole class is dead code)
- **File:** `ViewModels/PodcastDetailViewModel.swift:12`
- **Issue:** Grep for `PodcastDetailViewModel` across the app, ShareExtension, and tests finds only its own definition — it has no consumers. Within it, `import Combine` and `private var cancellables = Set<AnyCancellable>()` are also unused. The class duplicates content-loading logic that lives elsewhere.
- **Fix:** Remove PodcastDetailViewModel.swift entirely, or if a podcast detail screen is planned, at minimum drop the unused `cancellables`/`import Combine`.
- **Evidence:** Grep for `PodcastDetailViewModel` returns only its own definition (ViewModels/PodcastDetailViewModel.swift:12 plus the file-header comment at line 2). No instantiation in any view, tab coordinator, or test. The class is `@MainActor class ... : ObservableObject` with full content-loading logic that duplicates ContentService usage found elsewhere. Within it, `import Combine` (line 9) and `private var cancellables = Set<AnyCancellable>()` (line 21) are also unused. Confirmed dead class.

### [MEDIUM] swiftui — Summary decode runs JSONSerialization + JSONDecoder inside computed properties evaluated in body
- **File:** `Models/ContentDetail.swift:462`
- **Issue:** structuredSummary, editorialSummary, bulletedSummary, interleavedSummary, interleavedSummaryV2, and longformArtifact each perform JSONSerialization.data(withJSONObject:) followed by JSONDecoder().decode(...) every time the property is accessed (ContentDetail.swift lines 387-473). ContentDetailView.body (Views/ContentDetailView.swift ~lines 187-274) accesses these in a sequential `if let / else if let` chain, so each body evaluation re-serializes and re-decodes the raw summary dictionary up to several times (the structured branch is last, so all prior decode attempts also run). body re-evaluates on scroll and any @State change, making this repeated, non-cached, expensive work in the view-update path. This is the kind of per-render allocation that causes scroll jank on the detail screen.
- **Fix:** Decode each summary variant once (e.g. lazily memoized on the model, or computed once in the view model / .task and stored in @State), rather than re-decoding on every body pass. At minimum, resolve the summary kind first and decode only the matching variant.
- **Evidence:** CONFIRMED mechanism, severity overstated. ContentDetail.swift lines 387-473: each computed property (longformArtifact, interleavedSummary, interleavedSummaryV2, bulletedSummary, editorialSummary, structuredSummary) calls JSONSerialization.data(withJSONObject: raw.mapValues{...}) then JSONDecoder().decode(...) with NO caching. ContentDetailView.swift body (var body line 133, ScrollView) accesses these directly in the if-let/else-if-let chain at lines 187, 199, 211, 223, 235, 247 via viewModel.content (observed @Published), so body re-evaluates on @State/@Published changes and re-runs the work uncached. BUT the claim 'all prior decode attempts also run' is WRONG: every property guards on resolvedSummaryKind FIRST (line 389 guard =="longform_artifact", line 463 guard =="long_structured", etc.) and returns nil BEFORE any serialization when the kind doesn't match. So only the ONE matching branch serializes+decodes per body pass, not several. Real uncached per-render decode is a legit perf concern but limited to a single decode, not multiplied; medium not high.

### [MEDIUM] swiftui — newsMetadata/articleMetadata/podcastMetadata re-serialize + decode JSON on every access inside the view body
- **File:** `Models/ContentDetail.swift:253`
- **Issue:** `articleMetadata`, `podcastMetadata`, and `newsMetadata` each run `JSONSerialization.data(withJSONObject:)` followed by `JSONDecoder().decode(...)` on every access (lines 229-263). These are computed properties with no caching. The detail view body accesses them repeatedly per render: `content.newsMetadata` at ContentDetailView line 293, `content.sourceMetadata` (which calls both `articleMetadata` and `newsMetadata`) at line 276, and `content.detailTypeLabel` (calls `newsMetadata`) inside the hero header. Because the whole ScrollView has `.offset(x: dragAmount)` with `.animation(... value: dragAmount)` and the hero uses a GeometryReader driving parallax, the body recomputes frequently (every drag frame, every state change), so these full JSON round-trips run many times per second during swipe gestures.
- **Fix:** Decode the metadata once (e.g. lazily memoize on first decode, decode in the model's init, or compute it in the view model and store it) instead of re-deriving from `metadata` on each property access in the view body.
- **Evidence:** ContentDetail.swift: `metadata` is `[String: AnyCodable]` (line 58). articleMetadata (229-239), podcastMetadata (241-251), newsMetadata (253-263) each run `JSONSerialization.data(withJSONObject: metadata.mapValues { $0.value })` + `JSONDecoder().decode(...)` with no caching. sourceMetadata (265-273) calls BOTH articleMetadata and newsMetadata. These are accessed in the main body content tree: content.sourceMetadata (ContentDetailView.swift:276), content.newsMetadata (:293), content.detailTypeLabel→newsMetadata (:1145), all inside the `else if let content` branch (body line 144) under the top-level `.offset(x: dragAmount)` + `.animation(value: dragAmount)` (lines 415-416). Body re-evaluates on each dragAmount @State change during a drag gesture, re-running uncached JSON round-trips. Confirmed (note: re-runs per drag-update event, not literally every interpolated frame, but still a real repeated cost — medium is fair).

### [MEDIUM] swiftui — Card stack ForEach keyed by index, not content id — @State leaks across cards
- **File:** `Views/Components/LongFormCardStackView.swift:91`
- **Issue:** ForEach(visibleCardIndices, id: \.self) iterates over Int slot indices and renders items[index]; ArticleCardView is not given a stable .id(content.id). ArticleCardView holds @State (showDownloadSheet). When currentIndex advances the same slot index maps to a different ContentSummary, so per-card view state (e.g. an open download sheet) leaks onto whatever content now occupies that slot. Identity should follow the content, not the slot position.
- **Fix:** Drive the ForEach by content identity (e.g. ForEach(visibleCards, id: \.content.id)) or add `.id(content.id)` to the ArticleCardView so SwiftUI ties view state to the content, not the index.
- **Evidence:** LongFormCardStackView.swift line 91: `ForEach(visibleCardIndices, id: \.self)` iterates Int slot indices (visibleCardIndices returns Array of Int, lines 135-142), then renders items[index] (line 93). ArticleCardView is constructed at line 95 with NO .id(content.id) — grep confirms this is the only ArticleCardView( call site and it has no .id modifier. ArticleCardView holds @State private var showDownloadSheet (ArticleCardView.swift:28), used by a .sheet(isPresented:) at line 297. When currentIndex advances, the same slot index maps to a different ContentSummary, so per-card @State (e.g. an open download sheet) can leak onto whatever content now occupies that slot. Identity should follow content.id. Real, medium.

### [MEDIUM] swiftui — Repeating 5s Timer drives @Published mutations forever, even backgrounded
- **File:** `Services/UnreadCountService.swift:64`
- **Issue:** startPeriodicRefresh() schedules a repeating 5-second Timer that fires Task { await self.refreshCounts() }, which mutates @Published articleCount/podcastCount/newsCount. This singleton runs network requests and publishes view-tree-driving changes every 5 seconds for the entire app lifetime regardless of scene phase (foreground/background) or whether any view observes the counts. Combined with ProcessingCountService doing the same, this is continuous wakeups and redundant recomputation. The timer closure also retains the work via self; deinit invalidates but the singleton never deinits.
- **Fix:** Gate the timer on scenePhase == .active (start on foreground, invalidate on background), or convert to an async loop started/stopped by the owning view's .task lifecycle. At minimum pause refreshing when the app is not active.
- **Evidence:** UnreadCountService.swift line 63-69: `startPeriodicRefresh()` is called from `private init()` (line 45) and schedules `Timer.scheduledTimer(withTimeInterval: 5.0, repeats: true)` whose closure runs `Task { await self.refreshCounts() }`. refreshCounts (line 52-61) performs a network request via APIClient and mutates @Published articleCount/podcastCount/newsCount. It is a `static let shared` singleton (line 25) that never deinits, and a tree-wide grep shows no scenePhase gating anywhere — the timer runs every 5s for the whole app lifetime regardless of foreground/background or whether any view observes the counts. Real continuous-wakeup/churn concern; medium is appropriate.

### [MEDIUM] swiftui — Repeating 5s Timer publishes changes app-wide regardless of scene phase
- **File:** `Services/ProcessingCountService.swift:58`
- **Issue:** Same pattern as UnreadCountService: a repeating 5-second Timer fires Task { await self.refreshCount() } that mutates four @Published properties continuously for the app lifetime, independent of foreground/background state or whether any view is subscribed. This causes constant background network calls and view invalidation churn.
- **Fix:** Tie the polling to scenePhase/active foreground state or to a view's .task lifecycle so it stops when not needed, instead of an always-on Timer in a singleton.
- **Evidence:** ProcessingCountService.swift line 57-63: identical pattern — `startPeriodicRefresh()` called from `private init()` (line 38) schedules a 5.0s repeating Timer whose closure runs `Task { await self.refreshCount() }`. refreshCount (line 45-55) does an APIClient network request and mutates four @Published properties (processingCount, longFormProcessingCount, newsProcessingCount, newsCrawlCount). `static let shared` singleton (line 27), no scenePhase gating in the tree. Confirmed; medium appropriate.

### [MEDIUM] token — Submit button uses default .filled() tint (system blue) instead of app accent
- **File:** `../ShareExtension/ShareViewController.swift:235`
- **Issue:** configureSubmitButton() builds UIButton.Configuration.filled() without setting baseBackgroundColor, so the primary CTA inherits the system tint (blue) by default. This makes the most prominent button in the Share Extension render in the system blue accent rather than the app's terracotta brandPrimary, compounding the off-doctrine accent. Marked medium confidence because the resolved color depends on any inherited tintColor, but no app-accent tint is set anywhere in this controller.
- **Fix:** Set configuration.baseBackgroundColor (and baseForegroundColor for contrast) to the shared brandPrimary accent, or set the controller view's tintColor to the app accent so the filled button adopts it.
- **Evidence:** CONFIRMED. ShareExtension/ShareViewController.swift configureSubmitButton() (234-240): 'var configuration = UIButton.Configuration.filled()' (235), sets title/cornerStyle, then 'submitButton.configuration = configuration' (238); baseBackgroundColor never set. Grep for tint/baseBackgroundColor/brandPrimary/terracotta in the file returns only line 555 'indicatorView.tintColor = .systemBlue' (an activity indicator, not the button), and no app-accent tint is set on submitButton or the controller view. So the primary CTA renders with the default system tint (blue), not terracotta brandPrimary. Medium appropriate since resolved color depends on inherited tintColor, but no app accent applied. Extension has no SwiftUI Color tokens; fix sets baseBackgroundColor to a UIColor terracotta literal.

### [MEDIUM] token — `.fontWeight(.bold)` stacked on `.appLargeTitle` overrides the token's baked-in `.semibold`
- **File:** `Views/AuthenticationView.swift:32`
- **Issue:** `Text("Newsbuddy").font(.appLargeTitle).fontWeight(.bold)` — `appLargeTitle` is defined as `Font.appSerif(size: 34, relativeTo: .largeTitle, weight: .semibold)`. Re-applying `.fontWeight(.bold)` fights the token's intended semibold weight and produces a heavier Lora face than the design system specifies for large titles, creating an inconsistency with every other `appLargeTitle` usage. The weight override is invisible at the call site, so future token weight changes silently won't apply here.
- **Fix:** Drop the `.fontWeight(.bold)` and rely on `.appLargeTitle` as-is. If a bolder masthead weight is genuinely wanted, add a dedicated token (e.g. a bold display variant) in DesignTokens.swift rather than overriding at the call site.
- **Evidence:** CONFIRMED. AuthenticationView.swift 30-32: 'Text("Newsbuddy").font(.appLargeTitle).fontWeight(.bold)'. DesignTokens.swift line 229: 'static let appLargeTitle = Font.appSerif(size: 34, relativeTo: .largeTitle, weight: .semibold)'. The trailing .fontWeight(.bold) overrides the token's baked-in .semibold, producing a heavier Lora face than specified and diverging from other appLargeTitle usages; override invisible at call site so future token weight changes won't apply. Medium appropriate.

### [MEDIUM] token — `.fontWeight(.bold)` on the sheet `.appTitle3` header overrides the token's `.semibold`
- **File:** `Views/ContentDetailView.swift:1547`
- **Issue:** The sheet title `Text(title).font(.appTitle3).fontWeight(.bold)` overrides `appTitle3`'s baked-in `.semibold` (`Font.appSerif(size: 22, relativeTo: .title3, weight: .semibold)`). This yields a heavier serif weight than the standard title3 used elsewhere, so sheet titles render inconsistently against other title3 surfaces, and the override is hidden from token-level adjustments.
- **Fix:** Remove `.fontWeight(.bold)` so the sheet title matches the `appTitle3` token, or introduce a shared title token if bolder sheet headers are intended app-wide.
- **Evidence:** CONFIRMED (with detail correction). ContentDetailView.swift 1545-1547: 'Text(title).font(.appTitle3).fontWeight(.bold)'. DesignTokens.swift line 232: 'static let appTitle3 = Font.appSerif(size: 20, relativeTo: .title3, weight: .semibold)'. Trailing .fontWeight(.bold) overrides the token's .semibold, yielding a heavier serif sheet title than other title3 surfaces and hiding weight from token-level adjustments. NOTE: finding says appTitle3 is size 22; actual is size 20, a minor inaccuracy that does not affect the bold-overrides-semibold issue. Medium appropriate.

### [MEDIUM] token — ArticleCardView redefines card corner radius locally instead of CardMetrics.cardCornerRadius
- **File:** `Views/Components/ArticleCardView.swift:30`
- **Issue:** `private let cardCornerRadius: CGFloat = 20` (line 30) shadows the canonical CardMetrics.cardCornerRadius (24). This both bypasses the token and silently diverges from it (20 vs 24), so this card has a different corner radius than every other card surface (e.g. LongFormCard.swift:207 which correctly uses CardMetrics.cardCornerRadius). The local constant is used at lines 58, 60, 98, 101.
- **Fix:** Delete the local constant and reference CardMetrics.cardCornerRadius at lines 58/60/98/101. If 20 is intentional, add a named token (e.g. CardMetrics.compactCardCornerRadius) rather than a bare literal.
- **Evidence:** CONFIRMED. ArticleCardView.swift line 30: 'private let cardCornerRadius: CGFloat = 20' shadows the canonical token. DesignTokens.swift line 354: 'static let cardCornerRadius: CGFloat = 24' (CardMetrics). Local constant bypasses the token and diverges (20 vs 24); used at lines 58 (.cornerRadius), 60 (RoundedRectangle), 98 (topLeadingRadius), 101 (topTrailingRadius). This card renders a different corner radius than other card surfaces. Medium appropriate.

### [MEDIUM] token — ArticleCardView redefines hero image height locally instead of CardMetrics.heroImageHeight
- **File:** `Views/Components/ArticleCardView.swift:31`
- **Issue:** `private let heroImageHeight: CGFloat = 200` (line 31) duplicates the purpose of CardMetrics.heroImageHeight (180) and diverges in value. Used at lines 83, 95, 115. ContentCard.swift correctly pulls thumbnailSize from RowMetrics.thumbnailSize, so the pattern of sourcing card sizing from the token enum already exists; this view bypasses it.
- **Fix:** Reference CardMetrics.heroImageHeight, or if a taller hero is deliberate here, promote 200 to a named token (e.g. CardMetrics.tallHeroImageHeight) instead of a local literal.
- **Evidence:** ArticleCardView.swift:31 `private let heroImageHeight: CGFloat = 200`, used at lines 83, 95, 115 (`.frame(height: heroImageHeight)`). CardMetrics.heroImageHeight = 180 in DesignTokens.swift:353. Local constant duplicates the token's purpose and diverges in value (200 vs 180), a real token-bypass violation. Caveat: ArticleCardView is only consumed by LongFormCardStackView (LongFormCardStackView.swift:95), which is itself never instantiated externally (grep for `LongFormCardStackView(` returns nothing; name appears only inside its own file), so the host view may also be dead — but the token violation is independently valid. Medium severity appropriate.

